---
phase: 01-foundation-project-memory
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - api/src/app.ts
  - api/src/lib/errors.ts
  - api/src/app.test.ts
  - api/src/lib/logger.ts
  - api/src/lib/logger.test.ts
  - api/src/server.ts
autonomous: true
requirements: [FND-04]
coupling_justified:
  - "01-04: both run against buildApp(deps) and createLogger(options) frozen by the 01-01 tracer; 01-03 edits only app.ts internals, errors.ts, logger.ts internals and server.ts, while 01-04 edits only lib/coingecko.ts and routes/health.ts; each runs only its own test files, so either completion order yields the same result"
  - "01-05: api/ versus web/ file sets are disjoint and the smoke test picks free ports, so parallel smoke runs cannot collide"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A request with a valid UUID X-Request-Id (lowercase or uppercase) gets exactly that value echoed and logged; a missing, malformed, over-long, JSON-breaking or multi-valued header, or a value sent only under the alternate request-id header name, gets a freshly generated UUID and the rejected text never appears in log output"
    - "404 responses have body { error: { code: 'NOT_FOUND', message, requestId } } and unhandled errors return 500 with { error: { code: 'INTERNAL_ERROR', message: 'Internal Server Error', requestId } }; in both, requestId equals the X-Request-Id header, and the 500 body contains neither the original error message nor a stack trace (D-09)"
    - "Errors carrying a 4xx statusCode return that status with their own code (or VALIDATION_ERROR for schema validation failures) and message in the same D-09 shape"
    - "Each request produces exactly one `request completed` JSON line with requestId, method, path (query string removed), status, durationMs (non-negative number) and userId (null until Phase 2); a query value such as token=abc123 never appears in the logs (D-07)"
    - "A cross-origin request from an allowed origin receives Access-Control-Allow-Origin for that origin and Access-Control-Expose-Headers including X-Request-Id; a request from http://evil.example receives no Access-Control-Allow-Origin header"
    - "createLogger never emits the CoinGecko API key value (nor authorization or cookie header values) for any redacted path, replacing it with [REDACTED], proven by unit tests through the real createLogger (D-08)"
    - "File logs rotate daily or at 10 MB via pino-roll keeping 14 rotated files, and api/logs/api.log is a symlink to pino-roll's current.log so the D-06 path always resolves to the active file; creating the symlink twice is idempotent and a pre-existing regular api.log is preserved as api.legacy.log"
    - "SIGINT or SIGTERM closes the Fastify server (running onClose, which closes SQLite) before the process exits"
    - "Edge FND-04 (concurrency): under 20 concurrent requests every response's X-Request-Id is distinct and maps to exactly one `request completed` line with the matching path, so no ID is reused or cross-attributed"
  artifacts:
    - path: "api/src/lib/errors.ts"
      provides: "D-09 error body helper and Fastify not-found/error handlers"
      exports: ["ApiErrorBody", "errorBody", "registerErrorHandlers"]
    - path: "api/src/app.test.ts"
      provides: "Request-ID, error shape, CORS, request log line and concurrency tests"
      min_lines: 80
    - path: "api/src/lib/logger.ts"
      provides: "Redacting pino logger with pino-roll rotation and api.log symlink"
      exports: ["createLogger", "REDACT_PATHS", "buildRotationOptions", "ensureLogSymlink"]
    - path: "api/src/lib/logger.test.ts"
      provides: "Redaction and rotation/symlink unit tests"
      contains: "[REDACTED]"
  key_links:
    - from: "api/src/app.ts"
      to: "api/src/lib/errors.ts"
      via: "buildApp calls registerErrorHandlers(app)"
      pattern: "registerErrorHandlers\\("
    - from: "api/src/lib/logger.ts"
      to: "pino-roll"
      via: "transport target for the file log"
      pattern: "pino-roll"
    - from: "api/src/server.ts"
      to: "api/src/app.ts"
      via: "signal handlers call app.close()"
      pattern: "app\\.close\\("
---

<objective>
Harden the request-ID evidence thread that the tracer proved: strict reuse or replacement of incoming IDs, the single D-09 error shape carrying the request ID, a fully specified request log line (D-07), API-key redaction with a unit test (D-08), log rotation that keeps `api/logs/api.log` meaningful (D-06), and graceful shutdown.

Purpose: FND-04 and ROADMAP success criterion 3, made trustworthy enough that Phase 6 RCA write-ups can quote a request ID from a bug report and find exactly one matching log line.

Output: api/src/lib/errors.ts, api/src/app.test.ts, api/src/lib/logger.test.ts, and hardened api/src/app.ts, api/src/lib/logger.ts, api/src/server.ts.
</objective>

## Phase Goal

ROADMAP Goal (verbatim): "A running skeleton (web + api) with CI and traceable logs, plus the wiki memory and test strategy every later phase builds on". User story framing: NOT AVAILABLE (the Goal line is not in As a / I want to / so that form; run `/gsd mvp-phase 1` to add it; the planner did not invent one).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-project-memory/01-CONTEXT.md
@.planning/phases/01-foundation-project-memory/01-RESEARCH.md
@.planning/phases/01-foundation-project-memory/01-01-SUMMARY.md

<interfaces>
Frozen by plan 01-01 (do not change these signatures):
- api/src/app.ts: interface AppDeps { config: AppConfig; db: AppDatabase; logger: pino.Logger; fetchImpl?: typeof fetch; now?: () => number }; function buildApp(deps: AppDeps): Promise of FastifyInstance; const UUID_RE; function resolveRequestId(header: string | string[] | undefined): string; class RequestLogController extends LogController
- api/src/lib/logger.ts: function createLogger(options: { level?: string; filePath?: string | null; destination?: pino.DestinationStream }): pino.Logger
- api/src/config.ts: function loadConfig(env): AppConfig (fields include corsOrigins: string[], coingeckoApiKey: string | null, logFile: string | null)
- api/src/db/client.ts: function createDb(databasePath: string): { db: AppDatabase; close: () => void }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Request-ID reuse/replace, D-09 error shape, CORS expose/deny and the request log line, under test</name>
  <reversibility rating="costly">The { error: { code, message, requestId } } body is consumed by the web client, every later route, API tests and bug reports; changing it later touches all of them.</reversibility>
  <files>api/src/app.ts, api/src/lib/errors.ts, api/src/app.test.ts</files>
  <read_first>
    - api/src/app.ts (current genReqId, RequestLogController, onSend hook, CORS registration)
    - api/src/routes/health.test.ts (existing capture-stream test helper pattern to reuse)
    - api/src/lib/logger.ts
    - .planning/phases/01-foundation-project-memory/01-CONTEXT.md (D-07, D-09)
    - .planning/phases/01-foundation-project-memory/01-RESEARCH.md (Pattern 1 warning about unvalidated request-id headers, Pattern 2, Known Threat Patterns)
  </read_first>
  <behavior>
    - No X-Request-Id header: response header matches UUID_RE and equals the requestId on the request completed log line
    - Valid UUID header, lowercase and uppercase variants: echoed verbatim and logged verbatim
    - Rejected header values each produce a new UUID and never appear in captured log text: `not-a-uuid`; a 500-character string; a valid UUID followed by the suffix `","level":60,"x":"`; a script tag string; two X-Request-Id headers (array value); a valid UUID sent only under the header name request-id
    - GET /does-not-exist: 404, body error.code NOT_FOUND, error.requestId equals the response header
    - A test-only route that throws Error('boom internal detail'): 500, body exactly { error: { code: 'INTERNAL_ERROR', message: 'Internal Server Error', requestId } }, body text contains neither boom nor stack, and an error-level log line carries the error message
    - A test-only route that throws an error with statusCode 400 and code BAD_INPUT: 400 with error.code BAD_INPUT and its message
    - GET /health?token=abc123: exactly one request completed line with keys requestId, method GET, path /health, status 200, numeric durationMs of at least 0, userId null; captured log text never contains abc123
    - Origin http://localhost:5173 (in the default allow-list): access-control-allow-origin equals it and access-control-expose-headers contains x-request-id case-insensitively; Origin http://evil.example: no access-control-allow-origin header
    - 20 concurrent injects to /health: 20 distinct response IDs, each present in exactly one request completed line whose path is /health
  </behavior>
  <action>
Write `api/src/app.test.ts` first from the behaviour list (RED), then implement (GREEN). Build the app in tests with createDb(':memory:'), loadConfig({}) (no key, default CORS allow-list) and createLogger({ destination }) where destination pushes each written chunk into an array that the test parses line by line as JSON. Test-only throwing routes are added with app.get on the instance returned by buildApp before the first inject (Fastify allows route registration until ready).
Create `api/src/lib/errors.ts` (per D-09): export interface ApiErrorBody { error: { code: string; message: string; requestId: string } }; export `errorBody(code, message, requestId)`; export `registerErrorHandlers(app)` which (1) sets a not-found handler replying 404 with errorBody('NOT_FOUND', 'Route METHOD PATH not found' using the request method and the URL path without its query string, request.id), and (2) sets an error handler that, for a statusCode of 500 or above or no statusCode, logs through request.log at error level with the err object and message `unhandled error`, then replies 500 with errorBody('INTERNAL_ERROR', 'Internal Server Error', request.id), never including the original message or stack; and for a 4xx statusCode replies with that status and errorBody(code, error.message, request.id) where code is VALIDATION_ERROR when error.validation is present, otherwise error.code when it is a string, otherwise BAD_REQUEST.
Update `api/src/app.ts` without changing the exported signatures: call registerErrorHandlers(app) inside buildApp before routes are registered; add `app.decorateRequest('userId', null)` with a `declare module 'fastify'` augmentation adding `userId: string | null` to FastifyRequest (populated from Phase 2); make RequestLogController log `userId: request.userId ?? null` instead of the literal null; keep resolveRequestId strict (a single string matching UUID_RE, else crypto.randomUUID()) and keep genReqId reading only the x-request-id header, so the alternate request-id header name is never trusted (RESEARCH Pattern 1: Fastify's header option performs no validation). Keep the onSend hook so 404 and 500 responses also carry X-Request-Id, and keep CORS origin as the explicit allow-list with exposedHeaders ['X-Request-Id'].
Run `npx prettier --write` and `npx eslint` on the three files before committing.
  </action>
  <verify>
    <automated>npm --prefix api run test -- src/app.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or the summary line lacks "passed"</fails_when>
    <automated>npm --prefix api run typecheck && npx eslint api/src/app.ts api/src/lib/errors.ts api/src/app.test.ts</automated>
    <fails_when>non-zero exit, or any "error TS" line, or eslint reports an error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q "registerErrorHandlers" api/src/app.ts && grep -q "setNotFoundHandler" api/src/lib/errors.ts && grep -q "setErrorHandler" api/src/lib/errors.ts` exits 0
    - `grep -q "INTERNAL_ERROR" api/src/lib/errors.ts && grep -q "NOT_FOUND" api/src/lib/errors.ts` exits 0
    - `grep -q "decorateRequest" api/src/app.ts` exits 0
    - `grep -cE "(it|test)\(" api/src/app.test.ts` prints 9 or more
    - `npm --prefix api run test` (whole api suite, including 01-01's health.test.ts) exits 0
  </acceptance_criteria>
  <done>Every behaviour above is covered by a passing test in api/src/app.test.ts; 404/500 responses use the D-09 shape with the request ID; the request log line matches D-07 including the userId slot. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API-key redaction, pino-roll rotation behind api/logs/api.log, graceful shutdown</name>
  <files>api/src/lib/logger.ts, api/src/lib/logger.test.ts, api/src/server.ts</files>
  <read_first>
    - api/src/lib/logger.ts (current transport targets from 01-01)
    - api/src/server.ts
    - api/src/config.ts (logFile resolution and coingeckoApiKey field name)
    - .planning/phases/01-foundation-project-memory/01-CONTEXT.md (D-06, D-08)
    - .planning/phases/01-foundation-project-memory/01-RESEARCH.md (Pattern 3, Don't Hand-Roll rows for logging and rotation, Open Question 2)
    - node_modules/pino-roll/README.md (option names: file, size, frequency, extension, symlink, limit.count, dateFormat, mkdir)
  </read_first>
  <behavior>
    - createLogger({ destination }) logging { headers: { 'x-cg-demo-api-key': 'k-SECRET-1' } } writes [REDACTED] and never k-SECRET-1
    - Same for req.headers['x-cg-demo-api-key'] (k-SECRET-2), req.headers.authorization and req.headers.cookie values, a nested upstream.headers['x-cg-demo-api-key'] (k-SECRET-3), a top-level apiKey (k-SECRET-4), and { config: loadConfig({ COINGECKO_API_KEY: 'k-SECRET-5' }) } whose coingeckoApiKey is redacted
    - Non-secret fields in the same objects (for example url and status) are still present
    - buildRotationOptions('/abs/logs/api.log') returns file '/abs/logs/api', extension '.log', frequency 'daily', size '10m', dateFormat 'yyyy-MM-dd', limit { count: 14 }, mkdir true, symlink true
    - ensureLogSymlink on a temp dir path ending api.log creates a symlink named api.log pointing at current.log; calling it a second time succeeds and leaves the same symlink
    - ensureLogSymlink when a regular file api.log already exists renames it to api.legacy.log (content preserved) and then creates the symlink
  </behavior>
  <action>
Write `api/src/lib/logger.test.ts` first from the behaviour list (RED), using a capture destination and os.tmpdir() temp directories cleaned up after each test.
Update `api/src/lib/logger.ts` keeping the createLogger signature (per D-06, D-08): export REDACT_PATHS containing headers["x-cg-demo-api-key"], req.headers["x-cg-demo-api-key"], *.headers["x-cg-demo-api-key"], req.headers.authorization, req.headers.cookie, headers.authorization, headers.cookie, apiKey, *.apiKey, coingeckoApiKey and *.coingeckoApiKey; pass redact { paths: REDACT_PATHS, censor: '[REDACTED]' } in the pino options for both the destination (test) path and the transport path. Export `buildRotationOptions(filePath)` returning the pino-roll options exactly as in the behaviour list: file is filePath with its .log extension stripped, so pino-roll writes api.DATE.N.log beside a current.log symlink. Export `ensureLogSymlink(filePath)`: create the directory recursively; if filePath is already a symlink, do nothing; if it is a regular file, rename it to the same name with .legacy.log; then create a relative symlink at filePath pointing to current.log. When filePath is set, the transport's file target changes from pino/file to target pino-roll with buildRotationOptions(filePath), and ensureLogSymlink(filePath) runs before the transport is created; the stdout target stays pino/file to destination 1 (JSON only, no pretty printing). Do not hand-roll rotation (RESEARCH Don't Hand-Roll).
Update `api/src/server.ts`: register process.once handlers for SIGINT and SIGTERM that log `shutting down` with the signal, await app.close() (the existing onClose hook closes SQLite) and exit 0, exiting 1 with an error log if close rejects; add a 10-second fallback timer created with unref() that forces exit 1 if close hangs.
Run `npx prettier --write` and `npx eslint` on the three files. Then run `npm run smoke`: it points LOG_FILE at a temp api.log, so it now proves rotation plus the symlink end-to-end (the log correlation assertion reads through api.log to current.log).
  </action>
  <verify>
    <automated>npm --prefix api run test -- src/lib/logger.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or the summary line lacks "passed"</fails_when>
    <automated>npm run smoke</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK", or contains "SMOKE FAIL:"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q "pino-roll" api/src/lib/logger.ts && grep -q "REDACT_PATHS" api/src/lib/logger.ts && grep -q "current.log" api/src/lib/logger.ts` exits 0
    - `grep -q "x-cg-demo-api-key" api/src/lib/logger.ts` exits 0 (the redaction path list names the header)
    - `grep -q "SIGTERM" api/src/server.ts && grep -q "SIGINT" api/src/server.ts && grep -q "app.close" api/src/server.ts` exits 0
    - `grep -F "[REDACTED]" api/src/lib/logger.test.ts` matches
    - `npm --prefix api run test` exits 0 and `npm --prefix api run typecheck` exits 0
  </acceptance_criteria>
  <done>API key, authorization and cookie values are redacted by the real logger under unit test; file logs rotate daily or at 10 MB with 14 kept and api/logs/api.log resolves to the active file; the API shuts down gracefully; smoke still passes. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client to API | X-Request-Id, Origin and query strings are attacker-controlled |
| API to log files | Log lines are quoted into public bug reports and RCA write-ups |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Tampering / Repudiation | resolveRequestId in api/src/app.ts | high | mitigate | Strict UUID_RE reuse only; tests prove JSON-breaking, over-long, array and alternate-header values are replaced and never logged |
| T-01-12 | Information Disclosure | createLogger in api/src/lib/logger.ts | high | mitigate | pino redact over REDACT_PATHS with [REDACTED]; unit tests through the real createLogger for every path (D-08) |
| T-01-13 | Information Disclosure | registerErrorHandlers in api/src/lib/errors.ts | medium | mitigate | 500 body is the fixed message Internal Server Error; test asserts the original message and stack are absent; details go only to the log with the request ID |
| T-01-14 | Information Disclosure / Spoofing | @fastify/cors registration | medium | mitigate | Explicit allow-list; tests assert an unknown origin gets no Access-Control-Allow-Origin |
| T-01-15 | Denial of Service | file logging | medium | mitigate | pino-roll daily or 10 MB rotation with limit.count 14 bounds disk use |
| T-01-16 | Information Disclosure | RequestLogController path field | medium | mitigate | path strips the query string; test asserts a token query value never reaches logs |
</threat_model>

<verification>
- `npm --prefix api run test` passes, including app.test.ts, logger.test.ts and 01-01's health.test.ts
- `npm --prefix api run typecheck` and eslint on the changed files pass
- `npm run smoke` passes with rotation and the api.log symlink in place
</verification>

<success_criteria>
- ROADMAP success criterion 3 hardened: every response (200, 404, 500) carries X-Request-Id and the same ID is on exactly one request completed log line
- D-06, D-07, D-08 (redaction unit test), D-09 fully implemented and tested
</success_criteria>

## Edge Coverage (spec-less probe fallback)

- FND-04 / concurrency — resolved (explicit): 20 concurrent requests, distinct IDs, one matching log line each (must_haves truth "Edge FND-04").

## Prohibitions (spec-less recall)

- Canon referral, not minted: log injection through request headers and secrets in logs are canon OWASP logging items, covered by the threat register above (T-01-11, T-01-12) and /gsd-secure-phase.

## Artifacts this phase produces

Files: api/src/lib/errors.ts, api/src/app.test.ts, api/src/lib/logger.test.ts; modified api/src/app.ts, api/src/lib/logger.ts, api/src/server.ts.
Symbols: ApiErrorBody, errorBody, registerErrorHandlers (api/src/lib/errors.ts); REDACT_PATHS, buildRotationOptions, ensureLogSymlink (api/src/lib/logger.ts); FastifyRequest.userId request decoration with its module augmentation (api/src/app.ts); SIGINT/SIGTERM shutdown handlers (api/src/server.ts).
Error codes: NOT_FOUND, INTERNAL_ERROR, VALIDATION_ERROR, BAD_REQUEST.
Log messages: `request completed`, `unhandled error`, `shutting down`.
Runtime files: api/logs/api.log (symlink to current.log), api/logs/current.log, api/logs/api.YYYY-MM-DD.N.log, api/logs/api.legacy.log (only when a pre-rotation file existed).

<output>
Create `.planning/phases/01-foundation-project-memory/01-03-SUMMARY.md` when done.
</output>
