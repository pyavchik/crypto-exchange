---
phase: 01-foundation-project-memory
plan: 04
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - api/src/lib/coingecko.ts
  - api/src/lib/coingecko.test.ts
  - api/src/routes/health.ts
  - api/src/routes/health.test.ts
autonomous: true
requirements: [FND-03]
coupling_justified:
  - "01-03: both use buildApp(deps) and createLogger(options) frozen by 01-01; 01-04 edits only lib/coingecko.ts and routes/health.ts (plus their tests) and runs only its own test files, so completion order does not change either result"
user_setup:
  - service: coingecko
    why: "Optional in Phase 1 (health reports not_configured without a key); required from Phase 3 for market data"
    env_vars:
      - name: COINGECKO_API_KEY
        source: "CoinGecko developer dashboard -> Demo plan -> create API key; put it in api/.env (git-ignored)"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Without COINGECKO_API_KEY, GET /health reports upstream.coingecko { status: 'not_configured', checkedAt: null, latencyMs: null } and performs zero upstream calls and zero database reads or writes (D-04)"
    - "Upstream classification: 2xx at or under 2000 ms gives ok; 2xx slower than 2000 ms gives degraded; 429 gives degraded; 5xx gives down; timeout after 5000 ms gives down; network error gives down; any other non-2xx (for example 401 or 403) gives down (D-04)"
    - "A stored check younger than 300000 ms is served from upstream_checks without an upstream call; a check exactly 300000 ms old, or one whose checkedAt lies in the future, triggers a fresh check (D-05)"
    - "Failed checks are cached too: a down or degraded result is also reused for 5 minutes, so an outage never multiplies upstream calls"
    - "The cache survives an API restart: a second service instance over the same SQLite file reuses a fresh row without calling upstream"
    - "GET /health returns HTTP 200 whatever the upstream status, with top-level keys exactly status, version, commit, upstream; version equals api/package.json version and commit equals GIT_COMMIT or dev when unset (D-03)"
    - "Each real upstream call writes one log line with requestId, url (base URL + /ping, no query string), status, durationMs and result, and the configured API key value never appears in captured log output (D-08)"
    - "Edge FND-03 (concurrency): 10 concurrent /health requests against an expired cache trigger exactly one upstream /ping call, write exactly one upstream_checks row, and all 10 responses carry the same upstream result"
  artifacts:
    - path: "api/src/lib/coingecko.ts"
      provides: "Classification, SQLite-backed 5-minute cache, in-flight dedupe, timeout, outbound log line"
      exports: ["createCoingeckoStatusService", "classifyPing", "PING_CACHE_TTL_MS", "PING_TIMEOUT_MS", "SLOW_THRESHOLD_MS"]
    - path: "api/src/lib/coingecko.test.ts"
      provides: "Classification matrix, TTL boundary, clock skew, dedupe, restart, log-redaction tests"
      min_lines: 80
    - path: "api/src/routes/health.ts"
      provides: "GET /health contract (D-03)"
      exports: ["healthRoutes", "readApiVersion", "HealthResponse"]
    - path: "api/src/routes/health.test.ts"
      provides: "Route-level contract and concurrency tests"
      contains: "not_configured"
  key_links:
    - from: "api/src/lib/coingecko.ts"
      to: "api/src/db/schema.ts"
      via: "select newest row and insert after a real ping"
      pattern: "upstreamChecks"
    - from: "api/src/routes/health.ts"
      to: "api/src/lib/coingecko.ts"
      via: "getStatus({ requestId, log })"
      pattern: "getStatus\\("
  prohibitions:
    - statement: "MUST NOT call CoinGecko /ping on a timer or more than once per 5-minute window across all concurrent /health callers and API restarts; health checks must never spend the market-data call budget"
      status: resolved
      verification: test
    - statement: "MUST NOT report upstream ok (or any checkedAt/latencyMs) for a check that was not performed or did not succeed; missing key is not_configured with null fields, failures are down or degraded"
      status: resolved
      verification: test
---

<objective>
Make the `/health` upstream report exact and budget-safe: the full D-04 classification, the D-05 5-minute cache backed by SQLite (surviving restarts and deduplicating concurrent callers), the D-08 outbound log line without the key, and the D-03 response contract under route-level tests.

Purpose: FND-03. The badge and later monitoring must be able to trust the status, and a tab left polling must never threaten the ~10k/month Demo call budget (wiki/pages/concepts/market-data-caching.md).

Output: hardened api/src/lib/coingecko.ts and api/src/routes/health.ts with api/src/lib/coingecko.test.ts and expanded api/src/routes/health.test.ts.
</objective>

## Phase Goal

ROADMAP Goal (verbatim): "A running skeleton (web + api) with CI and traceable logs, plus the wiki memory and test strategy every later phase builds on". User story framing: NOT AVAILABLE (the Goal line is not in As a / I want to / so that form; run `/gsd mvp-phase 1` to add it; the planner did not invent one).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-project-memory/01-CONTEXT.md
@.planning/phases/01-foundation-project-memory/01-RESEARCH.md
@.planning/phases/01-foundation-project-memory/01-01-SUMMARY.md
@wiki/pages/concepts/market-data-caching.md
@wiki/pages/entities/coingecko-api.md

<interfaces>
Frozen by plan 01-01 (keep these signatures):
- api/src/lib/coingecko.ts: type UpstreamStatus = 'ok' | 'degraded' | 'down' | 'not_configured'; interface UpstreamCheck { status: UpstreamStatus; checkedAt: string | null; latencyMs: number | null }; interface CoingeckoStatusDeps { db: AppDatabase; apiKey: string | null; baseUrl: string; fetchImpl?: typeof fetch; now?: () => number }; interface CoingeckoStatusService { getStatus(ctx: { requestId: string; log: FastifyBaseLogger }): Promise of UpstreamCheck }; function createCoingeckoStatusService(deps); function classifyPing(input: { httpStatus: number | null; latencyMs: number; timedOut: boolean }); constants PING_CACHE_TTL_MS = 300000, PING_TIMEOUT_MS = 5000, SLOW_THRESHOLD_MS = 2000
- api/src/routes/health.ts: interface HealthResponse { status: 'ok'; version: string; commit: string; upstream: { coingecko: UpstreamCheck } }; function readApiVersion(): string; plugin healthRoutes(app, opts: { version; commit; coingecko })
- api/src/app.ts: buildApp(deps: { config, db, logger, fetchImpl?, now? })
- api/src/db/client.ts: createDb(databasePath): { db, close }; api/src/db/schema.ts: upstreamChecks (id, service, status, httpStatus, latencyMs, checkedAt, requestId)
- api/src/lib/logger.ts: createLogger({ level?, filePath?, destination? })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CoinGecko status service — classification matrix, 5-minute SQLite cache with exact boundaries, in-flight dedupe, key-free log line</name>
  <files>api/src/lib/coingecko.ts, api/src/lib/coingecko.test.ts</files>
  <read_first>
    - api/src/lib/coingecko.ts (tracer implementation to harden)
    - api/src/db/schema.ts
    - api/src/db/client.ts
    - api/src/lib/logger.ts
    - .planning/phases/01-foundation-project-memory/01-CONTEXT.md (D-04, D-05, D-08)
    - .planning/phases/01-foundation-project-memory/01-RESEARCH.md (Pattern 4, Common Pitfalls 4 and 5)
  </read_first>
  <behavior>
    - classifyPing table: (200, 150 ms) ok; (200, 2000 ms) ok; (200, 2001 ms) degraded; (429, 100 ms) degraded; (500) down; (503) down; (401) down; (403) down; (null status, timedOut true) down; (null status, timedOut false) down
    - apiKey null: getStatus returns not_configured with checkedAt null and latencyMs null; fetchImpl is never called; upstream_checks stays empty
    - Fake clock with now() controlled by the test: first call fetches once and inserts one row; at +299999 ms no fetch; at +300000 ms exactly one new fetch; a stored row with checkedAt 60 s in the future is treated as expired and refetched
    - A 503 result is cached: a second call 1 s later performs no fetch and returns down
    - fetchImpl that never resolves on its own: with PING_TIMEOUT_MS respected via an abort signal, the result is down with a non-null latencyMs (use a fetchImpl that rejects with a TimeoutError when its signal aborts; the test may use fake timers)
    - fetchImpl that throws TypeError('fetch failed'): result down, one row inserted with http_status null
    - 10 concurrent getStatus calls on an empty cache with a fetchImpl resolving after 50 ms: fetchImpl called exactly once, one row inserted, all 10 results deep-equal
    - Restart: two service instances created over the same temp-file database (createDb on a path in os.tmpdir()); the first performs one fetch; the second, 1 s later, performs zero fetches and returns the stored check
    - Outbound log: with apiKey 'test-secret-key-123' and a capture logger passed as ctx.log, exactly one line with msg upstream call, url equal to baseUrl + /ping, numeric status and durationMs, result ok, and requestId present; the captured text never contains test-secret-key-123
    - The request sent to fetchImpl has header x-cg-demo-api-key equal to the key and a URL without any query string
  </behavior>
  <action>
Write `api/src/lib/coingecko.test.ts` first (RED). For the request-scoped logger, create a capture logger with createLogger({ destination }) and pass `logger.child({ requestId: 'req-1' })` as ctx.log so the requestId binding is exercised like Fastify's request.log.
Then harden `api/src/lib/coingecko.ts` (per D-04, D-05, D-08) without changing exported signatures. Keep classifyPing exactly as the behaviour table (the 2000 ms boundary is inclusive for ok). In createCoingeckoStatusService keep a per-instance in-flight promise: when the cache is missing or expired and a check is already running, await that same promise instead of starting another fetch, and clear it in a finally block. Cache freshness is decided only from the newest upstream_checks row for service coingecko: age = now() minus Date.parse(checkedAt); fresh only when age is at least 0 and below PING_CACHE_TTL_MS, so a future timestamp or an exactly-TTL-old row triggers a refresh. Every result, including down and degraded, is stored and reused (budget protection). Detect a timeout by the error name AbortError or TimeoutError; anything else thrown is a network failure; both classify down with httpStatus null and a measured latency. Only lazy checks: there is no background timer or scheduled refresh in this module. Log the outbound call exactly once per real fetch through ctx.log at info (warn when the result is not ok) with msg upstream call and fields upstream coingecko, url (baseUrl + /ping), status, durationMs, result; never pass headers, the key or the deps object to the logger.
Run `npx prettier --write` and `npx eslint` on both files.
  </action>
  <verify>
    <automated>npm --prefix api run test -- src/lib/coingecko.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or the summary line lacks "passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cE "setInterval|setTimeout" api/src/lib/coingecko.ts` prints 0 (no timers; the timeout uses an abort signal)
    - `grep -q "AbortSignal.timeout" api/src/lib/coingecko.ts && grep -q "PING_CACHE_TTL_MS" api/src/lib/coingecko.ts` exits 0
    - `grep -F "test-secret-key-123" api/src/lib/coingecko.test.ts` matches (the redaction assertion exists)
    - `grep -cE "(it|test)\(" api/src/lib/coingecko.test.ts` prints 9 or more
    - `npm --prefix api run typecheck` exits 0
  </acceptance_criteria>
  <done>The status service classifies every D-04 case, caches every result for exactly 5 minutes in SQLite across restarts, never runs two concurrent pings, and logs each real call without the key; all covered by passing tests. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GET /health route contract — shape, version, commit, HTTP 200 on upstream failure, concurrent callers</name>
  <files>api/src/routes/health.ts, api/src/routes/health.test.ts</files>
  <read_first>
    - api/src/routes/health.ts
    - api/src/routes/health.test.ts (tests A and B from 01-01 must keep passing)
    - api/src/app.ts (how buildApp wires config.gitCommit and the service)
    - api/src/config.ts (gitCommit default)
    - api/package.json (version field)
    - .planning/phases/01-foundation-project-memory/01-CONTEXT.md (D-03)
  </read_first>
  <behavior>
    - No key: body deep-equals { status: 'ok', version: API_PKG_VERSION, commit: 'dev', upstream: { coingecko: { status: 'not_configured', checkedAt: null, latencyMs: null } } } where API_PKG_VERSION is read from api/package.json in the test; Object.keys(body) is exactly status, version, commit, upstream
    - loadConfig({ GIT_COMMIT: 'abc1234' }): commit is abc1234
    - Key set, fetchImpl resolving 503: HTTP status 200, upstream.coingecko.status down, checkedAt is an ISO-8601 string, latencyMs is a number
    - Key set, fetchImpl resolving 429: HTTP 200 with status degraded
    - Key set, fetchImpl rejecting with TypeError: HTTP 200 with status down
    - Key set, 10 concurrent injects on a fresh in-memory DB with a fetchImpl resolving 200 after 50 ms: fetchImpl called once, upstream_checks has one row, every body's upstream.coingecko deep-equals the first
    - Response content-type is application/json and x-request-id is present on every case above
  </behavior>
  <action>
Extend `api/src/routes/health.test.ts` with the behaviour list (RED where behaviour is missing), keeping 01-01's tests A and B.
Update `api/src/routes/health.ts` (per D-03) only as needed to satisfy the tests: the handler always replies 200 with exactly { status: 'ok', version, commit, upstream: { coingecko } } (the top-level status describes the API process; upstream health is reported separately and never turns /health into a failure status); readApiVersion reads api/package.json once at plugin registration, not per request; add a Fastify response schema for status 200 listing the four top-level properties with additionalProperties false and upstream.coingecko properties status (enum of the four statuses), checkedAt (string or null), latencyMs (number or null), so accidental extra fields cannot leak through serialization. Do not modify api/src/app.ts (owned by plan 01-03 in this wave); pass everything through the existing healthRoutes options.
Run `npx prettier --write` and `npx eslint` on both files, then `npm --prefix api run test` for the whole api suite.
  </action>
  <verify>
    <automated>npm --prefix api run test -- src/routes/health.test.ts</automated>
    <fails_when>non-zero exit, or "No test files found", or the summary line lacks "passed"</fails_when>
    <automated>npm --prefix api run test</automated>
    <fails_when>non-zero exit, or any test file reported as failed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q "additionalProperties" api/src/routes/health.ts && grep -q "not_configured" api/src/routes/health.ts` exits 0
    - `L=$(git log --grep='01-04' --format= --name-only) && test "$(printf '%s\n' "$L" | grep -c 'api/src/app.ts')" = 0` exits 0 (no 01-04 commit touches app.ts, which plan 01-03 owns in this wave)
    - `grep -cE "(it|test)\(" api/src/routes/health.test.ts` prints 8 or more
    - `npm --prefix api run typecheck` exits 0
  </acceptance_criteria>
  <done>GET /health always answers 200 with exactly the D-03 shape, correct version and commit, the precise upstream status, and one upstream call for any burst of concurrent callers; all covered by passing route tests. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any client to GET /health | Unauthenticated, can be called at any rate |
| API to CoinGecko | Key leaves the server; responses and latency are untrusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Denial of Service | createCoingeckoStatusService (call budget) | high | mitigate | SQLite-backed 5-minute cache for every result, per-instance in-flight dedupe, no timers; tests prove 10 concurrent callers and a restart cause at most one call |
| T-01-18 | Information Disclosure | outbound log line and request URL | high | mitigate | Key only in the x-cg-demo-api-key header; url logged as base + /ping; test asserts the key value is absent from captured logs |
| T-01-19 | Information Disclosure | /health response serialization | medium | mitigate | 200 response schema with additionalProperties false limits output to the D-03 fields |
| T-01-20 | Denial of Service | slow or hanging upstream | medium | mitigate | AbortSignal.timeout(5000) classifies as down; the result is cached so a hanging upstream cannot pin request workers repeatedly |
| T-01-21 | Tampering | upstream_checks writes | low | accept | Drizzle parameterizes all values and no client input reaches the insert except the server-generated request ID |
</threat_model>

<verification>
- `npm --prefix api run test` passes (coingecko.test.ts, health.test.ts, plus other api tests)
- `npm --prefix api run typecheck` passes
</verification>

<success_criteria>
- FND-03: GET /health returns version and upstream CoinGecko status with the exact D-03 shape
- D-04 and D-05 proven at their boundaries (2000 ms, 300000 ms, future timestamps, not_configured) and under concurrency
</success_criteria>

## Edge Coverage (spec-less probe fallback)

- FND-03 / concurrency — resolved (explicit): 10 concurrent requests, one upstream call, one row, identical results (must_haves truth "Edge FND-03").

## Prohibitions (spec-less recall, flagged-unverified)

- FND-03 / safety: never poll CoinGecko on a timer or beyond one call per 5 minutes (must_haves.prohibitions; descriptor-less).
- FND-03 / transparency: never report ok, checkedAt or latencyMs for a check that was not performed or failed (must_haves.prohibitions; descriptor-less).
- Canon referral, not minted: API key disclosure is a canon secrets item, covered by T-01-18 and /gsd-secure-phase.

## Artifacts this phase produces

Files: api/src/lib/coingecko.test.ts; modified api/src/lib/coingecko.ts, api/src/routes/health.ts, api/src/routes/health.test.ts.
Symbols: per-instance in-flight dedupe inside createCoingeckoStatusService; the 200 response schema for GET /health in healthRoutes. No new exports; the 01-01 exports keep their signatures.
Log message: `upstream call` (info when ok, warn otherwise).

<output>
Create `.planning/phases/01-foundation-project-memory/01-04-SUMMARY.md` when done.
</output>
