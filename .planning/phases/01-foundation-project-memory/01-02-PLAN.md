---
phase: 01-foundation-project-memory
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - eslint.config.js
  - .prettierrc.json
  - .prettierignore
  - .github/workflows/ci.yml
  - scripts/smoke-dev.mjs
  - api/drizzle.config.ts
  - api/src/config.ts
  - api/src/app.ts
  - api/src/server.ts
  - api/src/lib/logger.ts
  - api/src/lib/coingecko.ts
  - api/src/routes/health.ts
  - api/src/routes/health.test.ts
  - api/src/db/schema.ts
  - api/src/db/client.ts
  - web/vite.config.ts
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/lib/api.ts
  - web/src/lib/api.test.ts
  - web/src/components/HealthBadge.tsx
autonomous: true
requirements: [FND-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`npm run lint`, `npm run format:check`, `npm run typecheck` and `npm test` all exit 0 from the repo root"
    - "ESLint actually lints TypeScript: piping `const unused = 1` through eslint with stdin filename api/src/probe.ts exits 1 and reports @typescript-eslint/no-unused-vars"
    - "Prettier ignores docs folders: `npx prettier --file-info wiki/log.md` reports ignored true, so formatting can never rewrite the append-only wiki log"
    - "Public GitHub repository pyavchik/crypto-exchange exists, origin points at it, and the CI run for the pushed HEAD concludes success with jobs lint, typecheck and test each successful (success criterion 2)"
    - "No .env file and no CoinGecko key pattern (CG- followed by 16 or more alphanumerics) exists in any commit reachable from any ref before the first public push"
    - "Edge FND-02 (adjacency): ci.yml declares a concurrency group keyed on workflow and ref with cancel-in-progress true, so two pushes to the same ref in quick succession report the newest commit's run instead of two racing runs"
    - "Edge FND-02 (empty): root test and typecheck scripts name --workspace=api --workspace=web explicitly without the if-present flag, and Vitest's pass-with-no-tests option is not enabled anywhere; a workspace with a missing script or zero test files fails the gate (e.g. vitest run on an empty path exits non-zero) instead of passing vacuously"
    - "Edge FND-02 (ordering): lint, typecheck and test are separate jobs with no job-dependency edges, so each gate reports its own status and a lint failure never hides a test result"
  artifacts:
    - path: "eslint.config.js"
      provides: "ESLint 10 flat config: @eslint/js + typescript-eslint recommended + globals + eslint-config-prettier"
      contains: "typescript-eslint"
    - path: ".prettierignore"
      provides: "Keeps Prettier off wiki/, .planning/, qa/, Markdown and generated migrations"
      contains: "wiki"
    - path: ".github/workflows/ci.yml"
      provides: "GitHub Actions CI: lint, typecheck, test jobs on Node 24 with npm cache"
      contains: "node-version"
    - path: "package.json"
      provides: "Root lint, format, format:check scripts"
      contains: "format:check"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json"
      via: "jobs run npm run lint / format:check / typecheck / npm test"
      pattern: "npm run (lint|typecheck|format:check)"
    - from: "package.json"
      to: "eslint.config.js"
      via: "lint script runs eslint over the repo using the flat config"
      pattern: "eslint \\."
  prohibitions:
    - statement: "MUST NOT call the live CoinGecko API or reference any repository secret or CoinGecko key variable from CI; unit tests stub fetch and health tests run with no key (not_configured), so pushes never spend the Demo call budget"
      status: resolved
      verification: test
---

<objective>
Turn the proven skeleton into a gated, public project: lint and format gates that really run, a GitHub Actions workflow with independent lint, typecheck and test jobs on Node 24 (D-13), and the public repository `pyavchik/crypto-exchange` created and pushed with CI green (D-12).

Purpose: FND-02 and ROADMAP success criterion 2. Doing this immediately after the tracer means every wave-3 plan lands on a repo whose CI already catches regressions, and the native-module/Node 24 CI risk surfaces now instead of at phase end.

Output: eslint.config.js, .prettierrc.json, .prettierignore, root lint/format scripts, .github/workflows/ci.yml, public GitHub repo with a green CI run.
</objective>

## Phase Goal

ROADMAP Goal (verbatim): "A running skeleton (web + api) with CI and traceable logs, plus the wiki memory and test strategy every later phase builds on". User story framing: NOT AVAILABLE (the Goal line is not in As a / I want to / so that form; run `/gsd mvp-phase 1` to add it; the planner did not invent one).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-project-memory/01-CONTEXT.md
@.planning/phases/01-foundation-project-memory/01-RESEARCH.md
@.planning/phases/01-foundation-project-memory/SKELETON.md
@.planning/phases/01-foundation-project-memory/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lint and format gates that provably run over api, web and scripts</name>
  <files>package.json, eslint.config.js, .prettierrc.json, .prettierignore, scripts/smoke-dev.mjs, api/drizzle.config.ts, api/src/config.ts, api/src/app.ts, api/src/server.ts, api/src/lib/logger.ts, api/src/lib/coingecko.ts, api/src/routes/health.ts, api/src/routes/health.test.ts, api/src/db/schema.ts, api/src/db/client.ts, web/vite.config.ts, web/src/main.tsx, web/src/App.tsx, web/src/lib/api.ts, web/src/lib/api.test.ts, web/src/components/HealthBadge.tsx</files>
  <read_first>
    - package.json (current root scripts from 01-01)
    - .planning/phases/01-foundation-project-memory/01-RESEARCH.md (Common Pitfall 3: flat config only)
    - .planning/phases/01-foundation-project-memory/SKELETON.md (Tooling row)
    - wiki/SCHEMA.md (log.md is append-only, so Prettier must never touch wiki/)
  </read_first>
  <action>
Per D-13 and FND-02. The ESLint and Prettier packages were installed by plan 01-01; do NOT run npm install or change any dependency version in this task.
Create `eslint.config.js` (ESM; the root package.json already has type module) using `defineConfig` and `globalIgnores` imported from `eslint/config`. Global ignores: **/node_modules/**, **/dist/**, coverage/**, api/data/**, api/logs/**, api/src/db/migrations/**, .planning/**, wiki/**, qa/**, .claude/**. Config entries in this order: `js.configs.recommended` from @eslint/js; `tseslint.configs.recommended` from typescript-eslint; a block for api/**/*.ts, scripts/**/*.mjs and eslint.config.js with languageOptions.globals set to globals.node; a block for web/src/**/*.{ts,tsx} and web/vite.config.ts with globals.browser plus globals.node for the vite config; a rule block setting @typescript-eslint/no-unused-vars to error with argsIgnorePattern and varsIgnorePattern `^_`; and eslint-config-prettier as the final entry so formatting rules never conflict.
Create `.prettierrc.json` containing only printWidth 100 (all other Prettier defaults). Create `.prettierignore` with one entry per line: node_modules, package-lock.json, dist, coverage, api/data, api/logs, api/src/db/migrations, .planning, wiki, qa, .claude, and **/*.md. Markdown and docs folders are excluded because Prettier rewriting wiki/log.md would violate its append-only rule.
Add root scripts to package.json without touching existing ones: `lint` = `eslint .`, `format` = `prettier --write .`, `format:check` = `prettier --check .`. Keep the existing `test` and `typecheck` scripts exactly as 01-01 wrote them (explicit --workspace=api --workspace=web, no if-present flag).
Run `npm run format`, then `npm run lint`. Fix lint findings in the 01-01 files listed above without changing behaviour (for example remove unused imports, replace `any` with concrete types). If typescript-eslint reports an unsupported TypeScript version, stop: 01-01 must have pinned typescript to 6.0.x. Afterwards re-run `npm test` and `npm run smoke` to prove formatting did not change behaviour.
Prove the gates are not vacuous: pipe the text `const unused = 1` into `npx eslint --stdin --stdin-filename api/src/probe.ts` (must exit 1 with @typescript-eslint/no-unused-vars; never create that file), and run `npx prettier --file-info wiki/log.md` (must report ignored true).
  </action>
  <verify>
    <automated>npm run lint && npm run format:check</automated>
    <fails_when>non-zero exit, or eslint prints "error", or prettier prints "Code style issues found"</fails_when>
    <automated>printf 'const unused = 1\n' | npx eslint --stdin --stdin-filename api/src/probe.ts; test $? -eq 1</automated>
    <fails_when>the final test exits non-zero, meaning eslint exited 0 (TypeScript is not really being linted) or crashed with exit 2</fails_when>
    <automated>npx prettier --file-info wiki/log.md</automated>
    <fails_when>output does not contain "ignored": true</fails_when>
    <automated>npm test && npm run smoke</automated>
    <fails_when>non-zero exit, or the output lacks "SMOKE OK"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q "typescript-eslint" eslint.config.js && grep -q "eslint-config-prettier" eslint.config.js && grep -q "globalIgnores" eslint.config.js` exits 0
    - `grep -qx "wiki" .prettierignore && grep -qx ".planning" .prettierignore && grep -qx "api/src/db/migrations" .prettierignore` exits 0
    - `node -e "const s=require('./package.json').scripts; if(!s.lint||!s['format:check']||!s.format) process.exit(1)"` exits 0
    - `git grep -n -F -e '--if-present' -- package.json api/package.json web/package.json` prints nothing
    - `git grep -n passWithNoTests -- package.json api web` prints nothing
    - `npm --prefix api run test -- src/__no_such_dir__` exits non-zero (Vitest refuses to pass with zero test files)
    - `test ! -e api/src/probe.ts` exits 0
  </acceptance_criteria>
  <done>Lint, format check, typecheck and tests pass from the root; ESLint demonstrably lints TypeScript; Prettier demonstrably ignores wiki/. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: CI workflow with independent jobs, secret scan, public repo pyavchik/crypto-exchange, CI green</name>
  <reversibility rating="one-way">Once public, all history including .planning/ and wiki/ can be cloned and indexed; a leaked secret cannot be recalled. The user already chose this in D-12, so no decision checkpoint is inserted; the secret scan below is the guard.</reversibility>
  <precondition>`gh auth status` reports an active login to github.com as pyavchik</precondition>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-foundation-project-memory/01-CONTEXT.md (D-12, D-13)
    - .planning/phases/01-foundation-project-memory/01-RESEARCH.md (Code Example "GitHub Actions CI workflow", Pitfall 2)
    - package.json (script names the jobs call)
    - .gitignore
  </read_first>
  <action>
Per D-13, create `.github/workflows/ci.yml`: workflow name `CI`; triggers push on all branches and pull_request; top-level permissions with contents read only; top-level concurrency with group built from github.workflow and github.ref and cancel-in-progress true; top-level env GIT_COMMIT set to the github.sha expression (D-03 commit injection at CI time). Define three jobs with ids and names `lint`, `typecheck`, `test`, each on ubuntu-latest with these steps: actions/checkout@v4; actions/setup-node@v4 with node-version '24' and cache 'npm' (keys off the single root lockfile); `npm ci`; then the job's command: lint job runs `npm run lint` and `npm run format:check`; typecheck job runs `npm run typecheck`; test job runs `npm test`. The three jobs must not declare any dependency on each other, and the workflow must reference no repository secrets and no market-data key variable: tests stub fetch and the health route runs without a key.
Before any push (per D-12), run the secret scan and stop on any hit: (1) `git ls-files` must list no file named .env at any depth; (2) `git log --all --format= --name-only` must list no .env path in any commit; (3) scanning every commit with `git rev-list --all` piped to `xargs git grep -I -n -E 'CG-[A-Za-z0-9]{16,}'` must print nothing (market-data Demo API keys use this prefix; the history was clean on 2026-09-15). If anything is found, do not push: report the path and commit.
Create or attach the public repository: run `gh repo view pyavchik/crypto-exchange`. If it does not exist, run `gh repo create pyavchik/crypto-exchange --public --source=. --remote=origin --push --description "Binance-style paper-trading exchange on live crypto market data: a QA portfolio project (test plan, API tests, Playwright, RCA)"`. If it exists and has no commits, add it as remote origin with the URL gh reports and push the current branch with upstream tracking. If it exists with commits unrelated to this history, STOP and report; never force-push. The push intentionally publishes .planning/ and wiki/ (D-12).
Wait for CI on the pushed HEAD: poll `gh run list --repo pyavchik/crypto-exchange --commit` with the HEAD SHA and `--json databaseId` for up to 90 seconds until a run appears, then `gh run watch` that run with `--exit-status`. If a job fails, read `gh run view --log-failed`, fix the root cause (for example a better-sqlite3 native build issue on Node 24), commit, push, and watch again.
  </action>
  <verify>
    <automated>REVS=$(git rev-list --all) && test -n "$REVS" && { printf '%s\n' "$REVS" | xargs git grep -I -n -E 'CG-[A-Za-z0-9]{16,}'; test $? -eq 1; }</automated>
    <fails_when>non-zero exit: git rev-list failed or returned no commits, or git grep exited 0 (a key-like string was found in history) or 2 (the scan itself errored)</fails_when>
    <automated>RUN_ID=$(gh run list --repo pyavchik/crypto-exchange --commit "$(git rev-parse HEAD)" --limit 1 --json databaseId -q '.[0].databaseId'); test -n "$RUN_ID" && gh run watch "$RUN_ID" --repo pyavchik/crypto-exchange --exit-status</automated>
    <fails_when>non-zero exit, or RUN_ID is empty (no CI run exists for HEAD), or the watch output shows a job with conclusion failure</fails_when>
    <automated>gh run view "$(gh run list --repo pyavchik/crypto-exchange --commit "$(git rev-parse HEAD)" --limit 1 --json databaseId -q '.[0].databaseId')" --repo pyavchik/crypto-exchange --json jobs -q '.jobs[] | "\(.name)=\(.conclusion)"'</automated>
    <fails_when>output lacks any of the lines "lint=success", "typecheck=success", "test=success"</fails_when>
  </verify>
  <acceptance_criteria>
    - `gh repo view pyavchik/crypto-exchange --json visibility -q .visibility` prints PUBLIC
    - `git remote get-url origin` output contains pyavchik/crypto-exchange
    - `git status -sb` shows the current branch tracking origin with no "ahead" count after the push
    - `grep -q "cancel-in-progress: true" .github/workflows/ci.yml && grep -Eq "node-version: ['\"]?24['\"]?" .github/workflows/ci.yml && grep -Eq "cache: ['\"]?npm['\"]?" .github/workflows/ci.yml && grep -q "contents: read" .github/workflows/ci.yml` exits 0 (patterns accept single, double or no quotes because Prettier may requote YAML)
    - `grep -c "needs:" .github/workflows/ci.yml` prints 0
    - `grep -F "secrets." .github/workflows/ci.yml` prints nothing, and `grep -i "coingecko" .github/workflows/ci.yml` prints nothing
    - The job query prints lint=success, typecheck=success and test=success for HEAD
  </acceptance_criteria>
  <done>pyavchik/crypto-exchange is public with clean history, and CI for HEAD is green across the independent lint, typecheck and test jobs. Committed and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo to public GitHub | Every commit, including .planning/ and wiki/, becomes world-readable (one-way, D-12) |
| GitHub Actions runner to repo | CI executes repository code and third-party actions with a GITHUB_TOKEN |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Information Disclosure | first public push (Task 2) | high | mitigate | Secret scan across all commits (no .env ever tracked, no CG- key pattern) runs before `gh repo create --push`; stop on any hit, never push |
| T-01-08 | Elevation of Privilege | ci.yml GITHUB_TOKEN | medium | mitigate | Top-level `permissions: contents: read`; no write scopes, no secrets |
| T-01-09 | Tampering | third-party actions and npm install in CI | medium | mitigate | Only GitHub-owned actions/checkout@v4 and actions/setup-node@v4; `npm ci` installs exactly the committed lockfile vetted by the 01-01 legitimacy gate |
| T-01-10 | Information Disclosure / Denial of Service | CI calling CoinGecko | medium | mitigate | Workflow references no secrets or key variables; tests stub fetch; health runs as not_configured in CI, so pushes never spend the Demo budget (prohibition in must_haves) |
</threat_model>

<verification>
- `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm test` exit 0 locally
- Fail-first proofs: eslint exits 1 on an unused variable in a .ts stdin probe; prettier reports wiki/log.md ignored
- `gh repo view pyavchik/crypto-exchange` shows PUBLIC; the HEAD CI run has lint, typecheck and test jobs all successful
</verification>

<success_criteria>
- ROADMAP success criterion 2: CI goes green on GitHub for lint, typecheck and unit tests
- D-12 honored: public repo from day one, no secrets in history
- D-13 honored: push + PR triggers, install, lint, typecheck, unit tests, Node 24
</success_criteria>

## Edge Coverage (spec-less probe fallback)

- FND-02 / adjacency — resolved (explicit): concurrency group per workflow and ref with cancel-in-progress.
- FND-02 / empty — resolved (explicit): explicit workspace flags, no if-present flag, no pass-with-no-tests; Vitest exits non-zero on zero test files.
- FND-02 / ordering — resolved (explicit): independent lint, typecheck, test jobs with no job-dependency edges.

## Prohibitions (spec-less recall, flagged-unverified)

- FND-02 / safety: CI must never call live CoinGecko or reference secrets (in must_haves.prohibitions; descriptor-less, so it disposes flagged-unverified until a wired check exists).
- Canon referral, not minted: secret leakage into public CI logs and supply-chain pinning of actions are canon security items, covered by /gsd-secure-phase and the threat register above.

## Artifacts this phase produces

Files: eslint.config.js, .prettierrc.json, .prettierignore, .github/workflows/ci.yml; modified package.json (scripts lint, format, format:check); formatting-only edits to 01-01 source files.
CI: workflow `CI` with jobs `lint`, `typecheck`, `test`; workflow env GIT_COMMIT.
External: public GitHub repository pyavchik/crypto-exchange with git remote `origin`.

<output>
Create `.planning/phases/01-foundation-project-memory/01-02-SUMMARY.md` when done. Record the repo URL, the green CI run URL, and any lint fixes made to 01-01 files.
</output>
