---
phase: 01-foundation-project-memory
plan: 08
type: execute
wave: 4
depends_on: ["01-03", "01-04", "01-05", "01-06", "01-07"]
files_modified:
  - wiki/log.md
autonomous: true
requirements: [FND-01, FND-02, MEM-03]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "From a clean `npm ci`, `npm run lint`, `npm run format:check`, `npm run typecheck`, `npm test`, `npm run build --workspace=web`, `npm run smoke` and `node scripts/wiki-lint.mjs --base e954363` all exit 0 on the integrated wave-3 result"
    - "Every path listed in the qa/TEST-PLAN.md Phase 1 automated checks table exists in the repository"
    - "No wave-3 plan (01-03 to 01-07) changed package.json, api/package.json, web/package.json or package-lock.json, and only one package-lock.json exists"
    - "wiki/log.md ends with a LINT line for the Phase 1 transition carrying the counts printed by wiki-lint, and all earlier lines are unchanged"
    - "The final phase commit is pushed to origin and the CI run whose head SHA equals local HEAD concludes success with lint, typecheck and test jobs all successful (success criterion 2 on the finished phase)"
    - "No .env file and no CG- key pattern exists in any commit reachable from any ref at the moment of the final push"
  artifacts:
    - path: "wiki/log.md"
      provides: "Phase 1 transition LINT entry"
      contains: "| LINT |"
  key_links:
    - from: "wiki/log.md"
      to: "scripts/wiki-lint.mjs"
      via: "LINT line counts copied from the lint summary line"
      pattern: "LINT"
---

<objective>
Close Phase 1 by proving the five parallel wave-3 plans integrate: a clean install passes every gate, the smoke test passes on the combined code, documentation references resolve, the wiki gets its phase-transition LINT line, and the final commit is green on public CI.

Purpose: Parallel plans can each pass alone and still break together (formatting, shared test runs, doc paths). This gate makes success criteria 1-5 true on the actual final commit reviewers will see.

Output: appended LINT line in wiki/log.md, the final commit pushed with a green CI run.
</objective>

## Phase Goal

ROADMAP Goal (verbatim): "A running skeleton (web + api) with CI and traceable logs, plus the wiki memory and test strategy every later phase builds on". User story framing: NOT AVAILABLE (the Goal line is not in As a / I want to / so that form; run `/gsd mvp-phase 1` to add it; the planner did not invent one).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation-project-memory/SKELETON.md
@.planning/phases/01-foundation-project-memory/01-02-SUMMARY.md
@.planning/phases/01-foundation-project-memory/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrated clean-install gate, cross-artifact checks and the phase-transition wiki LINT line</name>
  <files>wiki/log.md</files>
  <read_first>
    - package.json (root scripts)
    - qa/TEST-PLAN.md (section Traceability, table "Phase 1 automated checks")
    - wiki/log.md (append only)
    - wiki/SCHEMA.md (LINT operation and log line format)
    - .claude/CLAUDE.md (phase-transition rule written by plan 01-06)
  </read_first>
  <action>
Run from the repo root, in order, stopping at the first failure: `npm ci`; `npm run lint`; `npm run format:check`; `npm run typecheck`; `npm test`; `npm run build --workspace=web`; `npm run smoke`; `node scripts/wiki-lint.mjs --base e954363`. If a gate fails because two wave-3 plans interact, fix the root cause in the failing file with the smallest behaviour-preserving change, re-run the whole sequence from `npm ci`, and record each fix as a deviation in the SUMMARY (this plan has no planned source edits).
Cross-artifact checks: (1) extract every repository path in backticks or table cells from the "Phase 1 automated checks" table in qa/TEST-PLAN.md and confirm each exists; fix the document if a path is wrong rather than creating a placeholder file. (2) Confirm `git log --format= --name-only --grep='01-0[3-7]'` lists none of package.json, api/package.json, web/package.json, package-lock.json. (3) Confirm exactly one package-lock.json is tracked. (4) Re-run the secret scan over all commits (no .env path ever tracked; no CG- key pattern).
Then perform the SCHEMA.md LINT for the Phase 1 transition: run wiki-lint, read the 7 new decision pages for contradictions with PROJECT.md and with each other (not machine-checked), and append to wiki/log.md one line using today's date from `date +%F`: `DATE | LINT | Phase 1 transition lint: P pages, O orphans, B broken links, U unverified mentions, C contradictions found | all`, where P, O, B and U come from the lint summary and C is the number of contradictions found by reading (flag, do not silently rewrite, any contradiction; if one needs a page change, make it and mention it in the SUMMARY). Run wiki-lint again to confirm the appended line keeps the log valid.
  </action>
  <verify>
    <automated>npm ci && npm run lint && npm run format:check && npm run typecheck && npm test && npm run build --workspace=web && npm run smoke</automated>
    <fails_when>non-zero exit at any step, or the output lacks "SMOKE OK"</fails_when>
    <automated>node scripts/wiki-lint.mjs --base e954363 && tail -n 1 wiki/log.md</automated>
    <fails_when>non-zero exit, or the lint summary shows errors other than 0, or the last log line does not contain "| LINT | Phase 1 transition lint"</fails_when>
    <automated>L=$(git log --format= --name-only --grep='01-0[3-7]') && { printf '%s\n' "$L" | grep -xE "package.json|api/package.json|web/package.json|package-lock.json"; test $? -eq 1; }</automated>
    <fails_when>non-zero exit: git log failed, or grep found a dependency manifest changed by a wave-3 plan (exit 0) or errored (exit 2)</fails_when>
  </verify>
  <acceptance_criteria>
    - `F=$(git ls-files) && test "$(printf '%s\n' "$F" | grep -c 'package-lock.json$')" = 1` exits 0
    - `for p in scripts/smoke-dev.mjs .github/workflows/ci.yml api/src/routes/health.test.ts api/src/lib/coingecko.test.ts api/src/app.test.ts api/src/lib/logger.test.ts scripts/wiki-lint.mjs; do test -f "$p" || exit 1; done` exits 0
    - `REVS=$(git rev-list --all) && test -n "$REVS" && { printf '%s\n' "$REVS" | xargs git grep -I -n -E 'CG-[A-Za-z0-9]{16,}'; test $? -eq 1; }` exits 0
    - `D=$(git diff e954363 -- wiki/log.md) && test "$(printf '%s\n' "$D" | grep -c '^-[^-]')" = 0` exits 0
    - `tail -n 1 wiki/log.md | grep -Eq "^[0-9]{4}-[0-9]{2}-[0-9]{2} \| LINT \| Phase 1 transition lint"` exits 0
  </acceptance_criteria>
  <done>The integrated phase passes every gate from a clean install, doc references resolve, dependency manifests were untouched by parallel plans, no secrets exist in history, and the Phase 1 transition LINT line is appended. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Push the final phase commit and confirm green CI on that exact SHA</name>
  <precondition>`git remote get-url origin` points at pyavchik/crypto-exchange (created by plan 01-02) and `gh auth status` shows an active login</precondition>
  <files>none (git push only)</files>
  <read_first>
    - .planning/phases/01-foundation-project-memory/01-02-SUMMARY.md (repo URL and how CI was watched)
    - .github/workflows/ci.yml
  </read_first>
  <action>
Push the current branch to origin (never force-push; if the push is rejected as non-fast-forward, fetch, inspect the remote commits, and stop and report rather than overwrite). Record `git rev-parse HEAD`. Poll `gh run list --repo pyavchik/crypto-exchange --commit` with that SHA and `--json databaseId,headSha,status` for up to 90 seconds until a run for exactly that SHA appears, then `gh run watch` it with `--exit-status`. If a job fails, read `gh run view --log-failed`, fix the root cause, commit, push and watch the new SHA's run. Record the run URL in the SUMMARY; it is the evidence for ROADMAP success criterion 2 on the finished phase.
  </action>
  <verify>
    <automated>SHA=$(git rev-parse HEAD); RUN_ID=$(gh run list --repo pyavchik/crypto-exchange --commit "$SHA" --limit 1 --json databaseId -q '.[0].databaseId'); test -n "$RUN_ID" && gh run watch "$RUN_ID" --repo pyavchik/crypto-exchange --exit-status</automated>
    <fails_when>non-zero exit, or RUN_ID is empty (no CI run for the local HEAD), or the watch reports a failed job</fails_when>
    <automated>gh run view "$(gh run list --repo pyavchik/crypto-exchange --commit "$(git rev-parse HEAD)" --limit 1 --json databaseId -q '.[0].databaseId')" --repo pyavchik/crypto-exchange --json headSha,jobs -q '.headSha, (.jobs[] | "\(.name)=\(.conclusion)")'</automated>
    <fails_when>the first line differs from `git rev-parse HEAD`, or output lacks any of "lint=success", "typecheck=success", "test=success"</fails_when>
  </verify>
  <acceptance_criteria>
    - `git status -sb | head -n 1` shows the branch tracking origin with no "ahead" or "behind" marker
    - The run view prints the local HEAD SHA and lint=success, typecheck=success, test=success
  </acceptance_criteria>
  <done>The final Phase 1 commit is on public GitHub and its CI run is green across lint, typecheck and test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo to public GitHub | Wave-3 commits (code, wiki, QA docs) become public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-31 | Information Disclosure | final push | high | mitigate | Task 1 re-runs the all-commit secret scan (.env paths, CG- key pattern) before Task 2 pushes; no force-push |
| T-01-32 | Repudiation | CI evidence | low | mitigate | Task 2 checks that the watched run's headSha equals local HEAD, so a green run for an older commit cannot be mistaken for the final one |
</threat_model>

<verification>
- Clean-install gate sequence passes, including SMOKE OK and wiki-lint errors=0
- CI run for the exact final SHA is green on lint, typecheck and test
</verification>

<success_criteria>
- ROADMAP success criteria 1-5 hold on the final pushed commit: dev + health badge (smoke plus 01-05 human check), CI green, X-Request-Id in logs, wiki files with ingests and LINT, qa/TEST-PLAN.md
</success_criteria>

## Artifacts this phase produces

Files: appended Phase 1 transition LINT line in wiki/log.md. No new symbols. External: CI run URL for the final SHA (recorded in 01-08-SUMMARY.md).

<output>
Create `.planning/phases/01-foundation-project-memory/01-08-SUMMARY.md` when done.
</output>
