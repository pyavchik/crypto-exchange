---
phase: 01-foundation-project-memory
plan: 11
type: execute
wave: 3
depends_on: ["01-09", "01-10"]
files_modified:
  - qa/bugs/BUG-001-health-badge-api-unreachable.md
  - wiki/pages/findings/health-poller-illegal-invocation.md
  - wiki/pages/decisions/tech-stack.md
  - wiki/log.md
  - qa/TEST-PLAN.md
  - qa/README.md
autonomous: true
gap_closure: true
gap_ids: [G-01-4]
requirements: [QA-01, MEM-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "qa/bugs/BUG-001-health-badge-api-unreachable.md fills every field and section of qa/templates/bug-report-template.md, is rated S2 Major / P1, and links the wiki finding. The wiki finding links BUG-001 back (CLAUDE.md findings rule)"
    - "The wiki finding records the fix (plan 01-09), the regression checks (unit tests plus the npm run smoke browser step, including the mutation-run result from plan 01-10) and a status saying retest is the Phase 1 UAT re-run"
    - "wiki/pages/decisions/tech-stack.md lists playwright-core 1.63 (library only, driving installed Chrome, used by npm run smoke) as a concrete Phase 1 choice. wiki/log.md gains exactly one appended FINDING line, and `node scripts/wiki-lint.mjs --base 7e14902` exits 0"
    - "qa/TEST-PLAN.md Priority section contains the sentence 'Every S1 and S2 bug is P1.', its independence example uses only S3/S4 defects, and no sentence lets an S1 or S2 defect sit below P1 (D-11)"
    - "qa/TEST-PLAN.md Test Approach has a Real-browser smoke row starting in Phase 1 (local Entry Criterion, CI job deferred to AUT-02 in Phase 6), and every path in its Phase 1 automated checks table exists in the repository"
    - "qa/README.md no longer describes qa/bugs/ as a folder that does not exist yet, and links BUG-001"
  artifacts:
    - path: "qa/bugs/BUG-001-health-badge-api-unreachable.md"
      provides: "Canonical bug report for the Phase 1 UAT blocker (D-10 format)"
      contains: "BUG-001"
    - path: "wiki/pages/findings/health-poller-illegal-invocation.md"
      provides: "RCA summary with fix, regression checks and back-link to BUG-001"
      contains: "BUG-001"
    - path: "wiki/pages/decisions/tech-stack.md"
      provides: "playwright-core recorded as a Phase 1 tool"
      contains: "playwright-core"
    - path: "qa/TEST-PLAN.md"
      provides: "Consistent priority rules and the Phase 1 real-browser check"
      contains: "Every S1 and S2 bug is P1."
    - path: "qa/README.md"
      provides: "Bugs folder pointer"
      contains: "BUG-001"
  key_links:
    - from: "qa/bugs/BUG-001-health-badge-api-unreachable.md"
      to: "wiki/pages/findings/health-poller-illegal-invocation.md"
      via: "relative markdown link in Root Cause and Fix"
      pattern: "wiki/pages/findings/health-poller-illegal-invocation\\.md"
    - from: "wiki/pages/findings/health-poller-illegal-invocation.md"
      to: "qa/bugs/BUG-001-health-badge-api-unreachable.md"
      via: "backticked repository path in the Status line"
      pattern: "qa/bugs/BUG-001-health-badge-api-unreachable\\.md"
    - from: "qa/TEST-PLAN.md"
      to: "scripts/smoke-dev.mjs"
      via: "Test Approach and Phase 1 automated checks rows"
      pattern: "smoke-dev\\.mjs"
---

<objective>
Finish UAT gap G-01-4 and the paper trail for BUG-001:
- file BUG-001 using the project template
- cross-link it with the wiki finding, both ways
- record the new playwright-core tool in the wiki
- fix the self-contradicting Priority section in qa/TEST-PLAN.md
- document the Phase 1 real-browser check that plan 01-10 added

Purpose: the QA documents are the headline deliverable. A reviewer should see a consistent severity/priority policy, a bug report that follows the project's own template, and an escape analysis showing the detection gap was closed where the bug slipped through.

Output: new `qa/bugs/BUG-001-health-badge-api-unreachable.md`; updated `qa/TEST-PLAN.md`, `qa/README.md`, the wiki finding, `wiki/pages/decisions/tech-stack.md`, and one appended `wiki/log.md` line.
</objective>

## Phase Goal

ROADMAP Goal (verbatim): "A running skeleton (web + api) with CI and traceable logs, plus the wiki memory and test strategy every later phase builds on". User story framing: NOT AVAILABLE (the Goal line is not in As a / I want to / so that form; the planner did not invent one).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@wiki/SCHEMA.md
@.planning/phases/01-foundation-project-memory/01-UAT.md
@.planning/phases/01-foundation-project-memory/01-09-SUMMARY.md
@.planning/phases/01-foundation-project-memory/01-10-SUMMARY.md
@qa/templates/bug-report-template.md
@qa/TEST-PLAN.md
@qa/README.md
@wiki/pages/findings/health-poller-illegal-invocation.md
@wiki/pages/decisions/tech-stack.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — BUG-001 filed and cross-linked with the wiki finding, wiki lint green</name>
  <files>qa/bugs/BUG-001-health-badge-api-unreachable.md, wiki/pages/findings/health-poller-illegal-invocation.md, wiki/pages/decisions/tech-stack.md, wiki/log.md</files>
  <read_first>
    - qa/templates/bug-report-template.md (every field and section, including the redaction note)
    - qa/TEST-PLAN.md (Severity and Priority sections, Evidence and Defect Workflow)
    - .planning/phases/01-foundation-project-memory/01-UAT.md (Execution Notes, tests 1-3, Gaps G-01-1..3)
    - .planning/phases/01-foundation-project-memory/01-09-SUMMARY.md (test names, RED excerpts, fix SHAs)
    - .planning/phases/01-foundation-project-memory/01-10-SUMMARY.md (mutation-run line, SMOKE OK run, Chrome version, SHAs)
    - wiki/pages/findings/health-poller-illegal-invocation.md
    - wiki/pages/decisions/tech-stack.md
    - wiki/SCHEMA.md (page format, FINDING operation, log line format)
  </read_first>
  <action>
Create qa/bugs/BUG-001-health-badge-api-unreachable.md by copying qa/templates/bug-report-template.md. Keep every heading and table field, and replace each placeholder (D-10 format).

Field values:
- Title (heading and ID/Title rows): Health badge shows "API unreachable" while GET /health returns 200.
- Severity: the cell starts with "S2 Major". Rationale: a core Phase 1 feature (ROADMAP success criterion 1) gives wrong results with no data corruption, which matches the TEST-PLAN S2 definition.
- Priority: the cell starts with "P1". Every S1/S2 bug is P1, and it blocks Phase 1 exit.
- Status: Fixed, with a note that the retest is the Phase 1 UAT re-run of tests 1-3.
- Environment: Local dev on an isolated copy (API :3100, web :5174, scratch SQLite), Google Chrome headed and driven by Playwright 1.63. Use the Chrome version recorded in 01-10-SUMMARY.md and the macOS version from `sw_vers -productVersion`.
- Build commit: the output of `git log -1 --format=%h 7e14902 -- web/src`, expected 36028fa. Note that local dev reports commit "dev" in GET /health unless GIT_COMMIT is set, so the SHA comes from git history.
- Request ID: N/A. The defect is client-side; every GET /health returned 200 and the UAT captured no request ids.
- Linked test case: none. Phase 1 has no TC files; found by Phase 1 UAT tests 1-3 (give the 01-UAT.md path).
- Linked requirement: FND-01 (web page shows API health status) and FND-03.
- Found in run: none. Found in Phase 1 UAT, not a scripted RUN report.
- Reporter: Claude, Phase 1 UAT run at the user's request.
- Date: 2026-09-15.

Sections:
- Summary: two sentences.
- Steps to Reproduce, at the build commit:
  1. Run `npm run dev`.
  2. Open http://localhost:5173 in Chrome with DevTools Console and Network (filter "health").
  3. Read the badge.
  4. Leave the tab visible for 70 s.
  5. Stop the API, click Re-check, restart the API and click Re-check again.
- Expected Result: "API ok" plus the CoinGecko status; "API unreachable" only while the API is down, recovering after restart; one poll every 60 s while visible.
- Actual Result: from UAT tests 1-3. Badge reads "API unreachable" with every /health at 200; console shows TypeError: Illegal invocation; no 60 s poll; no recovery after restart.
- Evidence: keep the redaction note. Screenshot: none captured; observations are recorded in 01-UAT.md. Network excerpt: every GET /health returned 200, no request id captured. Console excerpt: TypeError: Illegal invocation. Log excerpt: N/A, the API logged only successful 200 request lines and the defect lives in the browser.
- Impact: the badge reports a false outage, would hide a real one, and polling never runs.
- Root Cause and Fix:
  - one-paragraph root cause
  - a relative markdown link to ../../wiki/pages/findings/health-poller-illegal-invocation.md
  - fix commit SHAs and regression test names from 01-09-SUMMARY.md
  - the npm run smoke browser step plus the mutation-run result from 01-10-SUMMARY.md
  - a closing sentence: Phase 6 RCA-01 may expand this into a full RCA write-up

Update wiki/pages/findings/health-poller-illegal-invocation.md, keeping the SCHEMA page format:
- Set frontmatter updated to today's `date +%F`.
- Replace the Status line with: fixed by plan 01-09, detection added by plan 01-10, retest is the Phase 1 UAT re-run, bug report `qa/bugs/BUG-001-health-badge-api-unreachable.md` as a backticked repository path. Use [[slug]] links only for wiki pages.
- Add a "## Fix" section: timers are invoked as plain functions with lazy arrow-wrapper defaults; fulfilled and rejected handlers are split, so post-settlement exceptions and non-ApiError rejections propagate instead of rendering "API unreachable"; unparsable 2xx bodies become ApiError INVALID_RESPONSE_BODY (REVIEW WR-01/WR-02 folded in).
- Add a "## Regression checks" section: the Vitest strict-receiver tests (names from 01-09) and the npm run smoke browser step with its mutation-run result (from 01-10).
- Keep Symptom, Root cause and Lessons. Append two lessons:
  - Node tests can apply the browser receiver rule by stubbing the timer globals with strict-receiver functions.
  - Browser checks for timer behaviour must use real timers, because a fake page clock replaces native timers and hides receiver bugs.

Update wiki/pages/decisions/tech-stack.md:
- Add a Decision bullet: playwright-core 1.63 as a root devDependency, used as a library by the npm run smoke browser step and driving installed Google Chrome through the "chrome" channel (the bundled Chromium cannot be installed on macOS 13); added in Phase 1 gap closure after [[health-poller-illegal-invocation]].
- Add a Consequences bullet: the smoke needs Google Chrome installed locally and is not a CI job; Phase 6 AUT-02 builds its Playwright suite and CI job on the same library line.
- Add [[health-poller-illegal-invocation]] to related and set updated to today.
- Wiki pages must not contain anything shaped like an email address or a CG- key (wiki-lint sensitive-data check).

Append exactly one line to wiki/log.md and never touch earlier lines:
`<today> | FINDING | Health poller Illegal invocation fixed (01-09), real-browser step added to npm run smoke (01-10), BUG-001 filed | findings/health-poller-illegal-invocation, decisions/tech-stack`
  </action>
  <verify>
    <automated>node scripts/wiki-lint.mjs --base 7e14902</automated>
    <fails_when>non-zero exit, or the summary line shows errors= with a value other than 0</fails_when>
    <automated>bash -c 'B=qa/bugs/BUG-001-health-badge-api-unreachable.md; F=wiki/pages/findings/health-poller-illegal-invocation.md; for h in "## Summary" "## Steps to Reproduce" "## Expected Result" "## Actual Result" "## Evidence" "## Impact" "## Root Cause and Fix" "| **Severity** | S2 Major" "| **Priority** | P1" "| **Build commit** |" "| **Linked requirement** |"; do grep -qF "$h" "$B" || { echo "BUG-001 missing: $h"; exit 1; }; done; grep -qF "../../wiki/pages/findings/health-poller-illegal-invocation.md" "$B" && grep -qF "$B" "$F" && grep -qF "playwright-core" wiki/pages/decisions/tech-stack.md && tail -n 1 wiki/log.md | grep -q "| FINDING | .*BUG-001" && test "$(wc -l < wiki/log.md)" -eq "$(( $(git show 7e14902:wiki/log.md | wc -l) + 1 ))"'</automated>
    <fails_when>non-zero exit or a "BUG-001 missing:" line, meaning a template section or field is absent, a cross-link is missing in either direction, tech-stack lacks playwright-core, the last log line is not the new BUG-001 FINDING entry, or log.md did not grow by exactly one line since 7e14902</fails_when>
  </verify>
  <done>BUG-001 exists with every template field and section and is rated S2 Major / P1. It links the wiki finding, and the finding links it back with the fix and regression checks. tech-stack records playwright-core, one FINDING line is appended, and wiki-lint with --base 7e14902 reports errors=0.</done>
</task>

<task type="auto">
  <name>Task 2: qa/TEST-PLAN.md and qa/README.md — consistent priority policy and the Phase 1 real-browser check</name>
  <files>qa/TEST-PLAN.md, qa/README.md</files>
  <read_first>
    - qa/TEST-PLAN.md (Test Approach, Environments, Entry Criteria, Exit Criteria, Severity, Priority, Traceability, Deliverables and Status)
    - qa/README.md (Planned Folders, Phase 1 Automated Checks)
    - .planning/phases/01-foundation-project-memory/01-10-SUMMARY.md (browser step behaviour and smoke duration)
    - .planning/phases/01-foundation-project-memory/01-UAT.md (test 4 review notes a and b)
  </read_first>
  <action>
Edit qa/TEST-PLAN.md (D-10, D-11). Change only the parts below and keep every other section as is.

1. Priority section. Keep the P1/P2/P3 table. Replace the paragraph above the table and the example paragraph below it with exactly two short paragraphs.
   - First paragraph: Priority is urgency, Severity is impact, and the two are set independently with one fixed project rule. It must contain the exact sentence "Every S1 and S2 bug is P1." and tie that rule to the Exit Criteria requirement of 0 open S1 and 0 open S2 bugs.
   - Second paragraph: show independence using only S3 and S4 defects:
     - an S4 typo on the sign-up button that appears in the reviewer demo is P1
     - an S3 wrong colour for a negative 24h change on the markets table is P2
     - an S3 tie-break sort issue on a rarely used column is P3
   Remove the two existing sentences that let an S1 or S2 defect fall below P1: the hedged clause at the end of the first paragraph and the rare-load S2 example. Add no wording that lowers the priority of an S1 or S2 defect. Remove the repetition flagged in UAT test 4.

2. Test Approach table. Insert a row after "Full-stack smoke":
   - Level: Real-browser smoke
   - Tool: the `npm run smoke` browser step in `scripts/smoke-dev.mjs`, using `playwright-core` to drive installed Google Chrome headless
   - From phase: Phase 1
   - Notes: badge reads API ok / CoinGecko status with zero page or console errors; 60 s visible poll timing; outage and recovery through Re-check; real timers, no fake clock
   Below the table, before the per-phase QA cycle paragraph, add one paragraph:
   - Why it exists from Phase 1: BUG-001 passed the Node-environment unit tests and the fetch-only smoke because Node does not enforce browser host-object rules. Link qa/bugs/BUG-001-health-badge-api-unreachable.md.
   - Phases 2-5 must keep the step green before each phase UAT.
   - It is a local Entry Criterion, not a CI job (it needs Chrome and takes about the duration recorded in 01-10-SUMMARY.md). The Phase 6 Playwright suite (AUT-02, AUT-03) adds the CI job.

3. Environments, Browsers row: add that the npm run smoke browser step drives the installed Google Chrome.

4. Entry Criteria: extend the `npm run smoke` bullet to say it includes the real-browser step and needs Google Chrome installed.

5. Traceability, Phase 1 automated checks table:
   - FND-01 row becomes `scripts/smoke-dev.mjs` (`npm run smoke`, including the real-browser step)
   - FND-03 row additionally lists `web/src/lib/healthPoller.test.ts` and `web/src/lib/api.test.ts`

6. Deliverables and Status:
   - The Phase 1 automated checks row mentions the real-browser smoke step.
   - Add a row: `qa/bugs/BUG-001-health-badge-api-unreachable.md`, requirement FND-01, phase 1, status "Fixed (Phase 1), retest in UAT re-run".
   - Append "(BUG-001 already filed from Phase 1 UAT)" to the existing Bug reports QA-07 row, whose status stays Planned (Phase 6).

7. Set Last updated to today's `date +%F`.

Edit qa/README.md:
- In Planned Folders, remove the qa/bugs/ row and change the intro so it no longer says qa/bugs/ does not exist.
- Add a "## Bugs" section after Templates. It links bugs/BUG-001-health-badge-api-unreachable.md with a one-line description and says new reports follow the Defect Flow below.
- In Phase 1 Automated Checks, the npm run smoke row now also says it drives installed Google Chrome for the real-browser step (badge, 60 s poll, outage recovery). Keep the intro sentence about the Deliverables table accurate now that a BUG-001 row exists.
  </action>
  <verify>
    <automated>bash -c 'P=qa/TEST-PLAN.md; test "$(grep -c "could reasonably sit at P3" $P)" = 0 && test "$(grep -c "in principle be deprioritized" $P)" = 0 && grep -qF "Every S1 and S2 bug is P1." $P && grep -qF "Real-browser smoke" $P && grep -qF "qa/bugs/BUG-001-health-badge-api-unreachable.md" $P && grep -qF "BUG-001" qa/README.md'</automated>
    <fails_when>non-zero exit, meaning a contradictory priority sentence remains, the fixed-rule sentence or the Real-browser smoke row is missing, or BUG-001 is not referenced from TEST-PLAN.md and README.md</fails_when>
    <automated>bash -c 'for p in scripts/smoke-dev.mjs .github/workflows/ci.yml api/src/routes/health.test.ts api/src/lib/coingecko.test.ts api/src/app.test.ts api/src/lib/logger.test.ts scripts/wiki-lint.mjs web/src/lib/healthPoller.test.ts web/src/lib/api.test.ts qa/bugs/BUG-001-health-badge-api-unreachable.md; do test -e "$p" || { echo "missing $p"; exit 1; }; done'</automated>
    <fails_when>non-zero exit or a "missing" line, meaning a path cited in the TEST-PLAN Phase 1 automated checks or Deliverables tables does not exist</fails_when>
  </verify>
  <done>The Priority section states the S1/S2 to P1 rule once and uses only S3/S4 examples for independence. Test Approach, Environments, Entry Criteria, Traceability and Deliverables describe the Phase 1 real-browser smoke step and BUG-001. README treats qa/bugs/ as existing and links BUG-001. Every cited path exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository to public internet | The repo is public (D-12); anything committed in qa/ or wiki/ is world-readable and indexable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-41 | Information Disclosure | qa/bugs/BUG-001 evidence fields | medium | mitigate | Template redaction note kept. Evidence is limited to status codes, the console error text and paths. No cookies, Authorization headers, API keys, HAR files or screenshots with DevTools headers. The CoinGecko key never reaches the browser (T-01-18), so browser evidence cannot contain it. |
| T-01-42 | Information Disclosure | wiki page edits | low | mitigate | wiki-lint sensitive-data check (CG- key pattern and email shapes) runs in the Task 1 verify with --base 7e14902, which also proves wiki/raw is unchanged and log.md is append-only. |
| T-01-43 | Repudiation | wiki/log.md history | low | mitigate | Exactly one appended FINDING line. The --base 7e14902 lint fails if any earlier log line was rewritten. |
</threat_model>

<verification>
- `node scripts/wiki-lint.mjs --base 7e14902` exits 0 with errors=0
- BUG-001 has every template section and field and is cross-linked with the wiki finding in both directions
- qa/TEST-PLAN.md has no priority contradiction, and all its Phase 1 automated-check paths exist
- qa/README.md links BUG-001
</verification>

<success_criteria>
- G-01-4 closed: consistent Priority section, a real-browser check in the Phase 1 test approach, BUG-001 filed and linked to the wiki finding and back (the favicon item was delivered by plan 01-10)
- Wiki memory stays consistent (MEM-03 append-only log, tech-stack not stale about the new tool)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-project-memory/01-11-SUMMARY.md` when done.
</output>
