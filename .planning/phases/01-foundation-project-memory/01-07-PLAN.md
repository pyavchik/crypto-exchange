---
phase: 01-foundation-project-memory
plan: 07
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - qa/TEST-PLAN.md
  - qa/README.md
  - qa/templates/test-case-template.md
  - qa/templates/run-report-template.md
  - qa/templates/bug-report-template.md
  - .github/ISSUE_TEMPLATE/bug_report.md
autonomous: true
requirements: [QA-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "qa/TEST-PLAN.md has the sections Purpose, Scope, Out of Scope, Test Approach, Risks, Environments, Entry Criteria, Exit Criteria, Severity, Priority, Traceability, Evidence and Defect Workflow, Deliverables and Status (QA-01, D-10)"
    - "The Risks section is trading-aware: it has rows for decimal precision/rounding, fees, minimum notional and decimals validation, stale prices, rate limits/429 and the monthly call cap, limit-order crossing boundaries, locked funds and cancel release, concurrency/double-spend, cross-user isolation, session handling, API key exposure, average-cost P&L, and request-ID traceability, each with likelihood, impact, test focus, requirement IDs and the phase that tests it"
    - "Severity S1 Critical / S2 Major / S3 Minor / S4 Trivial and Priority P1 / P2 / P3 are defined with trading-specific examples, including wrong balance after a fill as S1 (D-11)"
    - "Entry and exit criteria are measurable (for example 0 open S1/S2 bugs and all P1 cases executed before a phase exits)"
    - "qa/templates/test-case-template.md uses a table whose header columns are exactly ID, Title, Req, Preconditions, Steps, Expected, Priority, Type, with IDs in the form TC-AREA-NNN and Type limited to positive / negative / boundary / security / UX (D-10)"
    - "qa/templates/bug-report-template.md captures steps, expected and actual results, severity, priority, environment, build commit, request ID, evidence (screenshot, network excerpt, log excerpt) and linked TC and REQ, and .github/ISSUE_TEMPLATE/bug_report.md contains exactly the same field names and section headings (D-10)"
    - "qa/templates/run-report-template.md records build commit, environment, per-TC Pass / Fail / Blocked / Not run results and linked bugs (D-10)"
  artifacts:
    - path: "qa/TEST-PLAN.md"
      provides: "Test strategy and plan for the whole project"
      contains: "## Exit Criteria"
    - path: "qa/templates/test-case-template.md"
      provides: "Per-feature test case table template"
      contains: "| ID | Title | Req | Preconditions | Steps | Expected | Priority | Type |"
    - path: "qa/templates/bug-report-template.md"
      provides: "Bug report template with request ID and evidence"
      contains: "Request ID"
    - path: "qa/templates/run-report-template.md"
      provides: "Execution report template"
      contains: "Blocked"
    - path: ".github/ISSUE_TEMPLATE/bug_report.md"
      provides: "GitHub issue form mirroring the bug template"
      contains: "Request ID"
    - path: "qa/README.md"
      provides: "Map of QA artifacts and naming conventions"
      contains: "TEST-PLAN.md"
  key_links:
    - from: "qa/TEST-PLAN.md"
      to: "qa/templates/"
      via: "Traceability and defect workflow sections link each template"
      pattern: "templates/"
    - from: ".github/ISSUE_TEMPLATE/bug_report.md"
      to: "qa/templates/bug-report-template.md"
      via: "field-for-field mirror"
      pattern: "Request ID"
  prohibitions:
    - statement: "MUST NOT claim executed test runs, coverage percentages or automation that do not exist yet; artifacts from later phases are listed as Planned with their phase"
      status: resolved
      verification: judgment
    - statement: "MUST NOT invite pasting unredacted secrets into public bug reports; both bug templates instruct removing API keys, session cookies and Authorization headers from screenshots, network and log excerpts"
      status: resolved
      verification: test
---

<objective>
Write the project's test strategy (`qa/TEST-PLAN.md`) and the reusable QA templates every later phase fills in: test cases, execution reports and bug reports, plus the GitHub issue template that mirrors the bug report (D-10, D-11).

Purpose: QA-01 and ROADMAP success criterion 5. The QA artifacts are the headline deliverable for a Senior QA application; the plan must read as trading-aware, honest about what is planned versus done, and tied to the request-ID evidence thread built in this phase.

Output: qa/TEST-PLAN.md, qa/README.md, qa/templates/{test-case,run-report,bug-report}-template.md, .github/ISSUE_TEMPLATE/bug_report.md.
</objective>

## Phase Goal

ROADMAP Goal (verbatim): "A running skeleton (web + api) with CI and traceable logs, plus the wiki memory and test strategy every later phase builds on". User story framing: NOT AVAILABLE (the Goal line is not in As a / I want to / so that form; run `/gsd mvp-phase 1` to add it; the planner did not invent one).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-project-memory/01-CONTEXT.md
@.planning/phases/01-foundation-project-memory/SKELETON.md
@wiki/pages/concepts/qa-portfolio-alignment.md
@wiki/pages/concepts/order-rules.md
@wiki/pages/concepts/market-data-caching.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: qa/TEST-PLAN.md — trading-aware scope, risks, environments, entry/exit criteria, severity/priority, traceability</name>
  <files>qa/TEST-PLAN.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (all v1 requirement IDs, Out of Scope table, v2 list)
    - .planning/ROADMAP.md (phase-to-requirement mapping and success criteria)
    - .planning/phases/01-foundation-project-memory/01-CONTEXT.md (D-10, D-11, D-07, D-09)
    - .planning/phases/01-foundation-project-memory/SKELETON.md (environments, ports, log path, CI jobs)
    - wiki/pages/concepts/order-rules.md (risk content and open questions)
    - wiki/pages/concepts/market-data-caching.md and wiki/pages/concepts/qa-portfolio-alignment.md
    - .github/workflows/ci.yml (job names from plan 01-02)
  </read_first>
  <action>
Per QA-01, D-10 and D-11, write `qa/TEST-PLAN.md` in plain GitHub-readable Markdown with a short header (project, document owner, last updated date, status Living document) and these H2 headings exactly, in order: `## Purpose`, `## Scope`, `## Out of Scope`, `## Test Approach`, `## Risks`, `## Environments`, `## Entry Criteria`, `## Exit Criteria`, `## Severity`, `## Priority`, `## Traceability`, `## Evidence and Defect Workflow`, `## Deliverables and Status`.
Scope: the v1 feature areas by requirement family (FND, MEM, AUTH, DATA, MKT, WAL, TRD, ORD, QA, AUT, RCA, SHIP) with the phase that delivers each. Out of Scope: the REQUIREMENTS.md Out of Scope table (real money/KYC, matching engine, futures/margin, OAuth/2FA, mobile apps, real-time tick data) and the v2 list, each with its reason.
Test Approach: levels and tools with their phase: unit tests (Vitest, from Phase 1), full-stack smoke (`npm run smoke`, Phase 1), manual scripted test cases per feature phase (Phases 2-5), API test collection (Bruno or Postman, Phase 6), Playwright TypeScript smoke with network mocking (Phase 6), charter-based exploratory testing and a UX review (Phase 6), release and post-release checklists (Phase 7); the per-phase cycle (write cases, execute, report, raise bugs, retest).
Risks: a table with columns ID, Risk, Trading impact, Likelihood, Impact, Test focus, Requirements, Phase, and rows R-01 onward covering at least: decimal precision and rounding (WAL-03, AUT-04; Phase 4); fee calculation and display (TRD-04; 4); minimum notional of 5 USDT, zero/negative and too-many-decimals validation (TRD-03; 4); stale prices during an upstream outage (DATA-03, MKT-05; 3); rate limits, 429s and the monthly Demo call cap (DATA-02, DATA-03, FND-03; 1 and 3; note the limits are unverified); limit-order crossing at exactly equal prices (TRD-06; 5); locked funds and exact release on cancel (TRD-05, TRD-07; 5); concurrency and double-spend on double-click or parallel requests (TRD-08; 5); cross-user data isolation and IDOR (AUTH-04; 2); session persistence and logout (AUTH-02, AUTH-03; 2); API key exposure to the browser or logs (DATA-01, FND-04; 1 and 3); average-cost realized and unrealized P&L (ORD-03, ORD-04; 5); request-ID traceability gaps that would block RCA (FND-04, RCA-01; 1 and 6); account reset side effects (WAL-04; 4). Keep open questions from order-rules (fill price for a crossed limit, fee asset, stale-price trading rule) as explicitly open.
Environments: local dev (web http://localhost:5173, API http://localhost:3000, SQLite at api/data/app.db, logs at api/logs/api.log, Node 24, started with `npm run dev`); CI (GitHub Actions ubuntu-latest, Node 24, jobs lint, typecheck, test); production on free hosting, provider decided in Phase 7 (Planned); browsers Chrome latest (primary) with Firefox and Safari spot checks; desktop 1280 px and mobile 390 px viewports; test data: fresh accounts per run, and CoinGecko either the Demo key or a local stub through COINGECKO_BASE_URL for deterministic 429 and stale scenarios.
Entry Criteria (per test cycle): phase requirements implemented and merged; CI green on the commit under test; the commit SHA recorded (visible as commit in GET /health); test cases written and reviewed against the requirements; `npm run smoke` passes on the environment; known blockers listed. Exit Criteria: all P1 cases and at least 90 percent of P2 cases executed; 0 open S1 and 0 open S2 bugs; every open S3/S4 bug has a report with priority and target phase; run report committed in qa/runs/; every requirement of the phase maps to at least one executed test case or passing automated test; CI green.
Severity (D-11): S1 Critical, S2 Major, S3 Minor, S4 Trivial, each with a definition and two or three trading examples: S1 includes wrong balance after a fill, double-spend on double-click, user A reading user B's wallet, API key visible in the browser; S2 includes a limit order not filling when the price crosses, cancel not releasing locked funds, no prices-delayed banner while serving stale prices; S3 includes wrong sort order for ties in the markets table, wrong colour for a negative 24h change, health badge not re-polling after returning to the tab; S4 includes typos and misaligned icons. Priority: P1 fix before the phase exits or a release, P2 fix in the current or next phase, P3 backlog; include one sentence and an example showing severity and priority are set independently.
Traceability: ID conventions REQ IDs from REQUIREMENTS.md, test cases TC-AREA-NNN in qa/test-cases/FEATURE.md with a Req column, run reports RUN-YYYY-MM-DD-SCOPE in qa/runs/, bugs BUG-NNN in qa/bugs/BUG-NNN-SLUG.md linking TC and REQ; link to qa/templates/. Add a table "Phase 1 automated checks" mapping FND-01 to scripts/smoke-dev.mjs (`npm run smoke`), FND-02 to .github/workflows/ci.yml, FND-03 to api/src/routes/health.test.ts and api/src/lib/coingecko.test.ts, FND-04 to api/src/app.test.ts and api/src/lib/logger.test.ts, MEM-01..04 to scripts/wiki-lint.mjs, QA-01 to review of this document; plan 01-08 verifies these paths exist.
Evidence and Defect Workflow: the request-ID thread: the UI shows the request ID on an API error, DevTools Network shows X-Request-Id, `grep` for that requestId in api/logs/api.log finds the log line, the bug report quotes it, and an RCA write-up (Phase 6) links log and network evidence to the fix and a regression test; bugs are canonical as qa/bugs files with an optional GitHub issue mirror. Include a redaction rule: never paste API keys, session cookies or Authorization headers into screenshots, HAR files or excerpts (the API redacts them in logs, but browser evidence is not redacted automatically).
Deliverables and Status: a table of artifact, requirement, phase, status. Only qa/TEST-PLAN.md, qa/templates/ and the Phase 1 automated checks are Done (Phase 1); every other artifact (test cases QA-02..05, exploratory notes QA-06, bug reports QA-07, API collection AUT-01, Playwright AUT-02/03, order-math tests AUT-04, RCA write-ups RCA-01, release checklists QA-08) is "Planned (Phase N)". Do not state any run results, pass rates or coverage numbers.
  </action>
  <verify>
    <automated>for h in "Purpose" "Scope" "Out of Scope" "Test Approach" "Risks" "Environments" "Entry Criteria" "Exit Criteria" "Severity" "Priority" "Traceability" "Evidence and Defect Workflow" "Deliverables and Status"; do grep -qx "## $h" qa/TEST-PLAN.md || { echo "missing: $h"; exit 1; }; done; echo headings-ok</automated>
    <fails_when>non-zero exit, or output contains "missing:", or "headings-ok" is not printed</fails_when>
    <automated>grep -cE "^\| R-[0-9]{2} " qa/TEST-PLAN.md</automated>
    <fails_when>the printed count is below 13, or grep exits non-zero (no risk rows)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q "S1" qa/TEST-PLAN.md && grep -q "S4" qa/TEST-PLAN.md && grep -q "P3" qa/TEST-PLAN.md && grep -qi "wrong balance after a fill" qa/TEST-PLAN.md` exits 0
    - `grep -qi "double-spend" qa/TEST-PLAN.md && grep -qi "stale" qa/TEST-PLAN.md && grep -q "429" qa/TEST-PLAN.md && grep -q "AUTH-04" qa/TEST-PLAN.md && grep -qi "rounding" qa/TEST-PLAN.md` exits 0
    - `grep -q "X-Request-Id" qa/TEST-PLAN.md && grep -q "api/logs/api.log" qa/TEST-PLAN.md` exits 0
    - `grep -c "Planned (Phase" qa/TEST-PLAN.md` prints 8 or more
    - `grep -Ei "[0-9]+ ?% (pass|coverage)|tests passed|executed on 20" qa/TEST-PLAN.md` prints nothing (no fabricated results)
    - `grep -Eqi "redact|never paste" qa/TEST-PLAN.md` exits 0
  </acceptance_criteria>
  <done>qa/TEST-PLAN.md defines scope, out of scope, approach, trading-aware risks, environments, measurable entry/exit criteria, S1-S4 and P1-P3 with trading examples, traceability and the request-ID evidence workflow, and marks all later-phase artifacts as Planned. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Test-case, run-report and bug-report templates, the mirrored GitHub issue template, and qa/README.md</name>
  <files>qa/templates/test-case-template.md, qa/templates/run-report-template.md, qa/templates/bug-report-template.md, .github/ISSUE_TEMPLATE/bug_report.md, qa/README.md</files>
  <read_first>
    - qa/TEST-PLAN.md (from Task 1: severity, priority, ID conventions, exit criteria to mirror)
    - .planning/phases/01-foundation-project-memory/01-CONTEXT.md (D-10 column and field lists)
    - .planning/phases/01-foundation-project-memory/01-PATTERNS.md (write the qa/templates bug template first, then mirror it)
  </read_first>
  <action>
Per D-10. Write `qa/templates/bug-report-template.md` first: an H1 of the form BUG-NNN followed by a short title; a two-column metadata table whose first-column cells are bold field names exactly: ID, Title, Severity, Priority, Status, Environment, Build commit, Request ID, Linked test case, Linked requirement, Found in run, Reporter, Date (with hint text such as S1-S4 per qa/TEST-PLAN.md, the commit value from GET /health, the X-Request-Id from the failing response); then H2 sections exactly: `## Summary`, `## Steps to Reproduce` (numbered), `## Expected Result`, `## Actual Result`, `## Evidence` (bullets for Screenshot, Network excerpt with the request, response status and X-Request-Id, and Log excerpt found by searching api/logs/api.log for the request ID), `## Impact`, `## Root Cause and Fix` (optional, link the RCA write-up and regression test). Directly under the Evidence heading add a blockquote: Redact before posting — remove API keys, session cookies and Authorization headers from screenshots, HAR files and excerpts; this repository is public.
Write `.github/ISSUE_TEMPLATE/bug_report.md` with GitHub issue-template frontmatter (name Bug report, about "Report a defect with reproduction steps and request-ID evidence", title "BUG: ", labels bug) followed by exactly the same bold field names, the same H2 headings in the same order and the same redaction blockquote, plus one line saying the canonical copy is filed as qa/bugs/BUG-NNN-SLUG.md.
Write `qa/templates/test-case-template.md`: a header with Feature, Area code (for example AUTH, MKT, WAL, TRD, ORD), Requirements covered, Last updated; usage notes (one file per feature at qa/test-cases/FEATURE.md; IDs TC-AREA-NNN never reused; Priority P1-P3; Type one of positive, negative, boundary, security, UX; multi-step Steps separated with HTML line-break tags inside a cell); then a table whose header row is exactly `| ID | Title | Req | Preconditions | Steps | Expected | Priority | Type |` followed by the separator row and one placeholder row using TC-AREA-001 with bracketed placeholders only (no real test content).
Write `qa/templates/run-report-template.md`: header fields Run ID (RUN-YYYY-MM-DD-SCOPE), Scope and phase, Build commit, Environment (URL, browser and version, OS, API version from GET /health), Tester, Start and end date; a summary table with rows Pass, Fail, Blocked, Not run, Total; a results table with columns TC ID, Title, Result, Bug, Notes where Result is one of Pass, Fail, Blocked, Not run; a Bugs raised list; an Exit criteria checklist copied from qa/TEST-PLAN.md Exit Criteria as unchecked Markdown checkboxes; Observations and risks; Sign-off.
Write `qa/README.md`: what lives in qa/ with links to TEST-PLAN.md and each template; the planned folders qa/test-cases/, qa/runs/ and qa/bugs/ with their naming conventions (created when their first file is written in Phases 2+, marked Planned); how a defect flows from test case to bug report to RCA; and the Phase 1 automated checks with their commands (`npm test`, `npm run smoke`, `node scripts/wiki-lint.mjs`).
  </action>
  <verify>
    <automated>grep -qxF "| ID | Title | Req | Preconditions | Steps | Expected | Priority | Type |" qa/templates/test-case-template.md && echo columns-ok</automated>
    <fails_when>non-zero exit, or "columns-ok" is not printed</fails_when>
    <automated>A=$(grep -oE '^\| \*\*[A-Za-z ]+\*\*|^## .+' qa/templates/bug-report-template.md); B=$(grep -oE '^\| \*\*[A-Za-z ]+\*\*|^## .+' .github/ISSUE_TEMPLATE/bug_report.md); test -n "$A" && test "$A" = "$B" && echo mirror-ok</automated>
    <fails_when>non-zero exit, or "mirror-ok" is not printed (the issue template's field names or section headings differ from the qa template, or the qa template has none)</fails_when>
  </verify>
  <acceptance_criteria>
    - `for f in ID Title Severity Priority Status Environment "Build commit" "Request ID" "Linked test case" "Linked requirement" "Found in run" Reporter Date; do grep -qF "| **$f**" qa/templates/bug-report-template.md || exit 1; done` exits 0
    - `for h in Summary "Steps to Reproduce" "Expected Result" "Actual Result" Evidence Impact "Root Cause and Fix"; do grep -qx "## $h" qa/templates/bug-report-template.md || exit 1; done` exits 0
    - `grep -qi "redact" qa/templates/bug-report-template.md && grep -qi "redact" .github/ISSUE_TEMPLATE/bug_report.md && grep -qi "cookie" .github/ISSUE_TEMPLATE/bug_report.md` exits 0
    - `head -n 6 .github/ISSUE_TEMPLATE/bug_report.md | grep -q "^name: " && head -n 6 .github/ISSUE_TEMPLATE/bug_report.md | grep -q "^labels: "` exits 0
    - `grep -q "Blocked" qa/templates/run-report-template.md && grep -q "Not run" qa/templates/run-report-template.md && grep -q "Build commit" qa/templates/run-report-template.md` exits 0
    - `grep -q "TEST-PLAN.md" qa/README.md && grep -q "templates/bug-report-template.md" qa/README.md` exits 0
  </acceptance_criteria>
  <done>Three QA templates with the D-10 columns and fields exist, the GitHub issue template mirrors the bug template field-for-field with the redaction warning, and qa/README.md maps the QA folder. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tester evidence to public repo / GitHub issues | Screenshots, HAR files and log excerpts become world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-29 | Information Disclosure | bug report templates and issue template | medium | mitigate | Redaction blockquote under Evidence in both templates (API keys, session cookies, Authorization headers); TEST-PLAN.md states the rule |
| T-01-30 | Repudiation | QA status claims | low | mitigate | Deliverables table marks later-phase artifacts Planned (Phase N); acceptance greps for fabricated pass rates |
</threat_model>

<verification>
- All 13 TEST-PLAN.md headings present; at least 13 risk rows
- Test-case column header exact; bug template and issue template field names and headings identical
</verification>

<success_criteria>
- ROADMAP success criterion 5: qa/TEST-PLAN.md defines scope, risks, severity/priority and entry/exit criteria
- D-10 and D-11 formats in place for Phases 2-7
</success_criteria>

## Flagged Assumptions (unresolved probe items — not silently dropped)

- QA-01 (unclassified, unresolved): assumed that a Markdown test plan reviewed against the D-10/D-11 checklist (required headings, risk rows, severity/priority examples, measurable criteria) satisfies QA-01. Whether the strategy is good enough for a Senior QA reviewer is a judgment the automated greps cannot make; it is left to the end-of-phase human review.

## Prohibitions (spec-less recall, flagged-unverified)

- QA-01 / transparency: no claims of executed runs, coverage or automation that do not exist (judgment-tier).
- QA-01 / privacy: bug templates must instruct redaction of secrets and cookies (test-tier).
- Canon referral, not minted: general handling of personal data in bug reports is a canon privacy item, covered by /gsd-secure-phase.

## Artifacts this phase produces

Files: qa/TEST-PLAN.md, qa/README.md, qa/templates/test-case-template.md, qa/templates/run-report-template.md, qa/templates/bug-report-template.md, .github/ISSUE_TEMPLATE/bug_report.md.
Conventions: IDs TC-AREA-NNN, RUN-YYYY-MM-DD-SCOPE, BUG-NNN, risk IDs R-NN; severity S1-S4; priority P1-P3; test types positive / negative / boundary / security / UX; results Pass / Fail / Blocked / Not run; bug statuses as listed in the template.

<output>
Create `.planning/phases/01-foundation-project-memory/01-07-SUMMARY.md` when done.
</output>
