---
phase: 01-foundation-project-memory
plan: 06
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - scripts/wiki-lint.mjs
  - .claude/CLAUDE.md
  - wiki/pages/decisions/paper-trading-scope.md
  - wiki/pages/decisions/limit-order-fill-model.md
  - wiki/pages/decisions/tech-stack.md
  - wiki/pages/decisions/coingecko-proxy.md
  - wiki/pages/decisions/qa-first-class.md
  - wiki/pages/decisions/llm-wiki-memory.md
  - wiki/pages/decisions/free-hosting-public-repo.md
  - wiki/index.md
  - .planning/PROJECT.md
  - wiki/log.md
autonomous: true
requirements: [MEM-01, MEM-02, MEM-03, MEM-04]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/wiki-lint.mjs --base e954363` exits 0 and prints pages=16 orphans=0 broken_links=0 duplicates=0 errors=0 after the backfill (MEM-01, MEM-02, MEM-03)"
    - "wiki/ keeps Karpathy's layers: wiki/raw/ with the 3 immutable sources unchanged since e954363, LLM-owned pages under wiki/pages/<type>/, and wiki/SCHEMA.md (MEM-01)"
    - "wiki/pages/decisions/ contains foundation-skeleton-conventions plus the 7 backfilled ADR pages paper-trading-scope, limit-order-fill-model, tech-stack, coingecko-proxy, qa-first-class, llm-wiki-memory, free-hosting-public-repo, each with SCHEMA.md frontmatter (title, type decision, updated, sources, related) and Context / Decision / Consequences sections (D-14)"
    - "wiki/index.md lists every one of the 16 pages exactly once under its category heading (concept pages may sit under Overview) (MEM-02)"
    - "wiki/log.md gains a DECISION line for the backfill and a LINT line with the counts reported by wiki-lint; every entry line matches `YYYY-MM-DD | INGEST|QUERY|LINT|DECISION|FINDING | summary | pages` (MEM-03)"
    - ".planning/PROJECT.md Key Decisions rows 1-7 each link to their wiki decision page, matching row 8's existing link style"
    - ".claude/CLAUDE.md tells the agent to read wiki/index.md before planning a phase and, at every phase transition, to run node scripts/wiki-lint.mjs and append a LINT line to wiki/log.md (MEM-04)"
    - "Edge MEM-02 (adjacency): no two files under wiki/pages/*/ share a slug, and no backfilled page duplicates foundation-skeleton-conventions (PROJECT.md Key Decisions row 8 maps to that existing page)"
    - "Edge MEM-02 (empty): index.md keeps the headings Overview, Sources, Entities, Concepts, Decisions and Findings, and any heading with no page bullets shows `_(none yet)_` instead of being dropped"
    - "Edge MEM-02 (ordering): under ## Decisions the pre-existing foundation-skeleton-conventions bullet stays first and the 7 new bullets follow in PROJECT.md Key Decisions table order"
    - "Edge MEM-04 (idempotency): running wiki-lint twice prints identical output, and each decision page path occurs exactly once in index.md, so a re-run of the backfill cannot create duplicate pages or bullets"
    - "Edge MEM-04 (concurrency): within Phase 1 only this plan and the 01-08 LINT append write under wiki/; the content of wiki/log.md at commit e954363 remains a byte-identical prefix of the current file (append-only), enforced by wiki-lint --base"
  artifacts:
    - path: "scripts/wiki-lint.mjs"
      provides: "Dependency-free LINT for wiki structure, frontmatter, links, index coverage, log format, raw immutability and append-only log"
      contains: "broken_links"
    - path: "wiki/pages/decisions/tech-stack.md"
      provides: "ADR for React+TS / Node+TS / SQLite with the concrete Phase 1 stack and TypeScript pin"
      contains: "type: decision"
    - path: "wiki/index.md"
      provides: "Catalog including the 7 new decision pages"
      contains: "pages/decisions/free-hosting-public-repo.md"
    - path: "wiki/log.md"
      provides: "Append-only DECISION and LINT entries for the backfill"
      contains: "| LINT |"
  key_links:
    - from: ".claude/CLAUDE.md"
      to: "scripts/wiki-lint.mjs"
      via: "phase-transition rule names the lint command"
      pattern: "wiki-lint"
    - from: ".planning/PROJECT.md"
      to: "wiki/pages/decisions/"
      via: "Key Decisions rows link to ADR pages"
      pattern: "wiki/pages/decisions/"
    - from: "wiki/index.md"
      to: "wiki/pages/decisions/"
      via: "Decisions section bullets"
      pattern: "pages/decisions/"
  prohibitions:
    - statement: "MUST NOT edit, rename or delete any file under wiki/raw/ or rewrite, reorder or delete any existing wiki/log.md line; provenance and the audit trail stay intact"
      status: resolved
      verification: test
    - statement: "MUST NOT state unverified CoinGecko limits, attribution terms or pricing as fact in new wiki pages; carry the (unverified) marker from the source pages"
      status: resolved
      verification: judgment
    - statement: "MUST NOT write API keys, secrets or personal contact details such as email addresses into wiki pages, which are published in the public repository"
      status: resolved
      verification: test
---

<objective>
Verify MEM-01..04 against the already-scaffolded wiki, automate the LINT operation, backfill the 7 PROJECT.md Key Decisions as ADR pages, catalog them, mirror the links in PROJECT.md, and append the DECISION and LINT log lines (D-14: do not rebuild what exists).

Purpose: The wiki is the durable memory every later phase consults; a repeatable, scripted LINT keeps it trustworthy at each phase transition, and the ADR pages give reviewers the "why" behind the architecture.

Output: scripts/wiki-lint.mjs, 7 new wiki/pages/decisions/*.md, updated wiki/index.md, wiki/log.md, .planning/PROJECT.md and .claude/CLAUDE.md.
</objective>

## Phase Goal

ROADMAP Goal (verbatim): "A running skeleton (web + api) with CI and traceable logs, plus the wiki memory and test strategy every later phase builds on". User story framing: NOT AVAILABLE (the Goal line is not in As a / I want to / so that form; run `/gsd mvp-phase 1` to add it; the planner did not invent one).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-project-memory/01-CONTEXT.md
@.planning/phases/01-foundation-project-memory/01-PATTERNS.md
@.planning/phases/01-foundation-project-memory/SKELETON.md
@wiki/SCHEMA.md
@wiki/index.md
@wiki/log.md
@wiki/pages/decisions/foundation-skeleton-conventions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scripted wiki LINT that verifies MEM-01..03 on the current wiki, plus the MEM-04 phase-boundary rule in CLAUDE.md</name>
  <files>scripts/wiki-lint.mjs, .claude/CLAUDE.md</files>
  <read_first>
    - wiki/SCHEMA.md (layers, page format, LINT operation, log line format)
    - wiki/index.md
    - wiki/log.md
    - wiki/pages/sources/karpathy-llm-wiki.md (a source page's frontmatter shape)
    - .claude/CLAUDE.md (section "Project Memory — LLM Wiki"; GSD-managed blocks between GSD:*-start and GSD:*-end markers must not be edited)
    - eslint.config.js (scripts/**/*.mjs uses node globals)
  </read_first>
  <action>
Per D-14 and MEM-01..04. Create `scripts/wiki-lint.mjs` (Node 24 ESM, no dependencies) run from the repo root. Flags: `--pages-only` skips index and log checks; `--base REV` enables git-based checks against that revision. Checks, each failure printed as a line starting with `ERROR` plus a code and detail:
(1) layers: wiki/SCHEMA.md, wiki/index.md, wiki/log.md exist; wiki/raw/ has at least one file; wiki/pages/ exists (code LAYER_MISSING).
(2) pages: every .md under wiki/pages/TYPE/ where TYPE is sources, entities, concepts, decisions or findings (any other subdirectory is PAGE_BAD_DIR); the frontmatter block must contain title, type, updated (YYYY-MM-DD), sources and related keys, and type must equal the singular of its directory: sources to source, entities to entity, concepts to concept, decisions to decision, findings to finding (FRONTMATTER).
(3) duplicate slugs (file names without .md) across type directories (DUPLICATE_SLUG).
(4) every double-bracket slug link inside wiki/pages/** and wiki/index.md must name an existing page slug (BROKEN_LINK); do not scan SCHEMA.md or log.md, whose link syntax examples are documentation.
(5) secrets and personal data in pages: a CG- key pattern (CG- plus 16 or more alphanumerics) or an email-address pattern in any page is an error (SENSITIVE); wiki/raw/ is not scanned because raw sources are immutable.
(6) index, unless --pages-only: headings Overview, Sources, Entities, Concepts, Decisions and Findings all present (INDEX_HEADING); every page path pages/TYPE/SLUG.md appears exactly once in index.md (missing is ORPHAN, repeated is INDEX_DUPLICATE); each bullet sits under its type's heading, except concept pages may sit under Overview (INDEX_CATEGORY); every index link target exists (INDEX_DEAD); a heading with no page bullets must contain the literal `_(none yet)_` (INDEX_EMPTY).
(7) log, unless --pages-only: after the header lines (the title, blank lines and the line starting with `Append-only.`), every non-blank line matches the pattern date, space-pipe-space, one of INGEST QUERY LINT DECISION FINDING, space-pipe-space, a summary, space-pipe-space, pages (LOG_FORMAT).
(8) with --base REV: `git diff --quiet REV -- wiki/raw` must exit 0 (RAW_MODIFIED), and the output of `git show REV:wiki/log.md` must be an exact prefix of the current wiki/log.md (LOG_REWRITTEN).
Finally print one summary line exactly in the form `wiki-lint: pages=N orphans=N broken_links=N duplicates=N unverified=N errors=N`, where unverified counts case-insensitive occurrences of the word unverified across pages (informational, never an error), and exit 1 when errors is above zero, else 0. Output must be deterministic (sort files) so two runs print identical text.
Run it against the current wiki: `node scripts/wiki-lint.mjs --base e954363` must report pages=9 and errors=0 with no wiki edits (the wiki was scaffolded correctly at init). If it reports errors on existing pages, fix the checker when the rule misreads SCHEMA.md, or report the genuine wiki defect in the SUMMARY and fix that page in Task 3; never weaken a rule to hide a real defect.
Edit only the "Project Memory — LLM Wiki" section of `.claude/CLAUDE.md` (outside every GSD marker block; per MEM-04): keep the existing bullets, and change the phase-transition bullet so it says to run `node scripts/wiki-lint.mjs --base` with the commit where the previous phase ended, read pages for contradictions and stale facts (not machine-checked), fix or flag findings, and append a LINT line with the printed counts to wiki/log.md. Run `npx eslint scripts/wiki-lint.mjs` and `npx prettier --write scripts/wiki-lint.mjs`.
  </action>
  <verify>
    <automated>node scripts/wiki-lint.mjs --base e954363</automated>
    <fails_when>non-zero exit, or the summary line is missing, or it shows errors= with a value other than 0, or pages= other than 9 at this point</fails_when>
    <automated>node scripts/wiki-lint.mjs --base e954363 --pages-only</automated>
    <fails_when>non-zero exit, or the summary line is missing</fails_when>
    <automated>npx eslint scripts/wiki-lint.mjs && npx prettier --check scripts/wiki-lint.mjs</automated>
    <fails_when>non-zero exit, or eslint prints an error, or prettier prints "Code style issues found"</fails_when>
  </verify>
  <acceptance_criteria>
    - Fail-first proof, run in a scratch copy and never committed: `T=$(mktemp -d) && cp -R wiki scripts "$T"/ && printf '\nnot a log line\n' >> "$T/wiki/log.md" && (cd "$T" && node scripts/wiki-lint.mjs --pages-only >/dev/null) && (cd "$T" && node scripts/wiki-lint.mjs >/dev/null; test $? -eq 1)` exits 0 (--pages-only still passes while the full run exits 1 on LOG_FORMAT)
    - `node scripts/wiki-lint.mjs` run twice produces byte-identical output (compare with `diff` of the two captured outputs)
    - `grep -q "wiki-lint" .claude/CLAUDE.md && grep -q "wiki/index.md" .claude/CLAUDE.md` exits 0
    - `git diff --quiet e954363 -- wiki/raw` exits 0
    - `git diff --stat -- wiki/` shows no changes from this task
  </acceptance_criteria>
  <done>A deterministic, dependency-free wiki LINT exists, passes on the current 9-page wiki with raw sources and log history verified against e954363, demonstrably fails on a malformed log line, and CLAUDE.md names it in the phase-transition rule. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill ADR pages for Key Decisions 1-5</name>
  <files>wiki/pages/decisions/paper-trading-scope.md, wiki/pages/decisions/limit-order-fill-model.md, wiki/pages/decisions/tech-stack.md, wiki/pages/decisions/coingecko-proxy.md, wiki/pages/decisions/qa-first-class.md</files>
  <read_first>
    - wiki/pages/decisions/foundation-skeleton-conventions.md (exact analog: frontmatter, Status line, Context / Decision / Consequences)
    - .planning/PROJECT.md (Key Decisions table rows 1-5, Constraints, Out of Scope)
    - .planning/REQUIREMENTS.md (Out of Scope table, TRD-05..08)
    - wiki/pages/concepts/order-rules.md (open questions to keep open)
    - wiki/pages/entities/coingecko-api.md and wiki/pages/concepts/market-data-caching.md ((unverified) markers)
    - wiki/pages/concepts/qa-portfolio-alignment.md
    - .planning/phases/01-foundation-project-memory/SKELETON.md and package-lock.json (actual installed versions for tech-stack)
  </read_first>
  <action>
Per D-14, create five pages copying the analog's structure exactly: frontmatter keys title, type decision, updated (today's date from `date +%F`), sources, related; an H1; a line `**Status:** Accepted (project init, 2026-09-15).` followed by where the decision lives (PROJECT.md Key Decisions row N); then `## Context`, `## Decision` (bullets) and `## Consequences` (bullets). Keep each page concise (roughly 15-35 lines), factual, cross-linked with double-bracket slugs that already exist, and mark anything unverified with (unverified). Do not duplicate foundation-skeleton-conventions (it already records Key Decision row 8).
- `paper-trading-scope.md` (row 1): simulated paper-trading exchange with a Binance-style UI because CoinGecko is market data only and a real exchange needs licensing and custody; spot only; out of scope per REQUIREMENTS.md (real money, KYC, matching engine, futures/margin, mobile, OAuth/2FA). Consequences: no custody risk; correctness of balances, fills and P&L is the QA story. sources: []; related: project-overview, coingecko-api, order-rules.
- `limit-order-fill-model.md` (row 2): limit orders fill when the reference price crosses the limit (buy when price is at or below the limit, sell when at or above), checked on each price refresh; no user-to-user matching engine. Consequences: deterministic and testable; fills happen only as often as prices refresh (about a minute, (unverified) upstream cache); the open questions in order-rules (fill price, fee asset, stale-price rule) stay open for Phases 4-5 and must not be answered here. sources: []; related: order-rules, coingecko-api, market-data-caching.
- `tech-stack.md` (row 3): React + TypeScript (Vite) / Node + TypeScript / SQLite, one language across app and Playwright automation. Decision bullets list the concrete Phase 1 choices from SKELETON.md with the versions actually in package-lock.json: npm workspaces, Vite + React + react-router, Fastify with pino, better-sqlite3 + Drizzle with committed migrations, Vitest in both workspaces, ESLint flat config + typescript-eslint + Prettier, GitHub Actions on Node 24; TypeScript pinned to 6.0.x because typescript-eslint 8.70 supports only below 6.1. Consequences: Playwright TS in Phase 6 shares the language; the SQLite choice constrains Phase 7 hosting to a persistent volume or a Postgres switch. sources: []; related: project-overview, foundation-skeleton-conventions, free-hosting-public-repo.
- `coingecko-proxy.md` (row 4): the backend proxies and caches every CoinGecko call; the Demo key never reaches the browser; limits of about 30 calls/min and 10k calls/month are (unverified). Decision bullets: header x-cg-demo-api-key server-side only; base URL overridable for stubs; Phase 1 applies it to /health's /ping with a 5-minute SQLite cache; the Phase 3 market-data TTL and refresh strategy is still open. Consequences: rate limits respected for any number of clients; 429/timeout become deliberate test scenarios (DATA-03, AUT-03). sources: [raw/2026-09-15-coingecko-demo-api.md]; related: coingecko-api, market-data-caching, foundation-skeleton-conventions.
- `qa-first-class.md` (row 5): the target role is QA, so QA artifacts are first-class deliverables: every feature phase ships FE + BE + manual test cases, plus a dedicated QA hardening phase. Decision bullets: qa/ layout (TEST-PLAN.md, test-cases, runs, bugs, templates), TC-AREA-NNN and BUG-NNN IDs, S1-S4 / P1-P3. Consequences: reviewers can trace REQ to test case to run to bug to RCA. sources: [raw/2026-09-15-railsware-senior-qa-tradezella.md]; related: qa-portfolio-alignment, railsware-senior-qa-tradezella, foundation-skeleton-conventions.
The pages are not yet in index.md (Task 3 adds them), so verify with --pages-only here.
  </action>
  <verify>
    <automated>node scripts/wiki-lint.mjs --pages-only</automated>
    <fails_when>non-zero exit, or pages= other than 14, or broken_links= other than 0, or any ERROR line (FRONTMATTER, DUPLICATE_SLUG, SENSITIVE)</fails_when>
  </verify>
  <acceptance_criteria>
    - `for s in paper-trading-scope limit-order-fill-model tech-stack coingecko-proxy qa-first-class; do grep -q "^type: decision$" wiki/pages/decisions/$s.md && grep -q "^## Context" wiki/pages/decisions/$s.md && grep -q "^## Decision" wiki/pages/decisions/$s.md && grep -q "^## Consequences" wiki/pages/decisions/$s.md || exit 1; done` exits 0
    - `grep -qi "unverified" wiki/pages/decisions/coingecko-proxy.md` exits 0
    - `grep -q "typescript" wiki/pages/decisions/tech-stack.md && grep -Eq "6\.0" wiki/pages/decisions/tech-stack.md` exits 0
    - `grep -qi "open" wiki/pages/decisions/limit-order-fill-model.md && grep -q "order-rules" wiki/pages/decisions/limit-order-fill-model.md` exits 0
    - `git diff --quiet e954363 -- wiki/raw` exits 0
  </acceptance_criteria>
  <done>Five ADR pages for Key Decisions 1-5 exist in the SCHEMA.md decision format with valid links and no sensitive data. Committed.</done>
</task>

<task type="auto">
  <name>Task 3: Backfill Key Decisions 6-7, catalog all 7 in index.md, mirror links in PROJECT.md, append DECISION and LINT log lines</name>
  <files>wiki/pages/decisions/llm-wiki-memory.md, wiki/pages/decisions/free-hosting-public-repo.md, wiki/index.md, .planning/PROJECT.md, wiki/log.md</files>
  <read_first>
    - wiki/pages/decisions/foundation-skeleton-conventions.md
    - wiki/SCHEMA.md (Operations and Log line format)
    - wiki/pages/sources/karpathy-llm-wiki.md
    - wiki/index.md (existing ## Decisions bullet format)
    - wiki/log.md (existing lines; append only)
    - .planning/PROJECT.md (Key Decisions table; row 8 shows the link style)
    - .planning/STATE.md (Blockers: hosting must support persistent SQLite or switch to free Postgres)
  </read_first>
  <action>
Per D-14. Create two more pages in the same format as Task 2:
- `llm-wiki-memory.md` (row 6): Karpathy's LLM Wiki pattern for durable project memory because of user preference and knowledge that compounds instead of being re-derived; Decision bullets: raw/ immutable, pages/ LLM-owned, SCHEMA.md conventions, index.md catalog, append-only log.md, `.planning/` holds execution state while wiki/ holds what was learned and why, LINT scripted as `node scripts/wiki-lint.mjs`. Consequences: every phase transition runs the LINT; decisions are mirrored as ADR pages plus PROJECT.md rows. sources: [raw/2026-09-15-karpathy-llm-wiki.md]; related: karpathy-llm-wiki, project-overview.
- `free-hosting-public-repo.md` (row 7): deploy on free hosting with a public repo so reviewers can click through, within the $0 budget. Status line: Accepted; public repository done in Phase 1 (pyavchik/crypto-exchange, D-12); hosting provider pending Phase 7. Decision bullets: repo public from day one, no secrets committed (.env ignored, .env.example tracked, history scan before first push); hosting must support a persistent SQLite volume or the project switches to free Postgres (open, decide in Phase 7 planning). Consequences: all planning and wiki history is public; the deploy choice is constrained by the SQLite decision. sources: []; related: tech-stack, foundation-skeleton-conventions, project-overview.
Update `wiki/index.md`: under `## Decisions`, keep the existing foundation-skeleton-conventions bullet first and add seven bullets directly below it, in PROJECT.md table order, each in the existing format `- [Title](pages/decisions/SLUG.md) — one-line summary`: paper-trading-scope, limit-order-fill-model, tech-stack, coingecko-proxy, qa-first-class, llm-wiki-memory, free-hosting-public-repo. Change nothing else in index.md (Findings keeps `_(none yet)_`).
Update `.planning/PROJECT.md` Key Decisions rows 1-7 only: append to each Rationale cell a link in row 8's style, `([wiki](../wiki/pages/decisions/SLUG.md))`, using the matching slug; do not alter decision text, outcomes or any other section.
Append to `wiki/log.md` (append only, never edit existing lines), using today's date from `date +%F`: first a line `DATE | DECISION | Backfilled 7 PROJECT.md Key Decisions as ADR pages | decisions/paper-trading-scope, decisions/limit-order-fill-model, decisions/tech-stack, decisions/coingecko-proxy, decisions/qa-first-class, decisions/llm-wiki-memory, decisions/free-hosting-public-repo, index`; then run `node scripts/wiki-lint.mjs --base e954363` and append `DATE | LINT | Phase 1 memory backfill lint: P pages, O orphans, B broken links, U unverified mentions flagged | all` with P, O, B and U taken from that run's printed counts. Run the lint again afterwards: it must still pass (the new lines match the log format and the old content is still a prefix).
  </action>
  <verify>
    <automated>node scripts/wiki-lint.mjs --base e954363</automated>
    <fails_when>non-zero exit, or the summary line lacks "pages=16", "orphans=0", "broken_links=0", "duplicates=0" and "errors=0", or any ERROR line is printed</fails_when>
    <automated>awk '/^## Decisions/{f=1;next} /^## /{f=0} f && /^- \[/' wiki/index.md | sed -E 's#.*pages/decisions/([a-z0-9-]+)\.md.*#\1#' | paste -sd, -</automated>
    <fails_when>output is not exactly foundation-skeleton-conventions,paper-trading-scope,limit-order-fill-model,tech-stack,coingecko-proxy,qa-first-class,llm-wiki-memory,free-hosting-public-repo</fails_when>
  </verify>
  <acceptance_criteria>
    - `for s in paper-trading-scope limit-order-fill-model tech-stack coingecko-proxy qa-first-class llm-wiki-memory free-hosting-public-repo foundation-skeleton-conventions; do test "$(grep -c "pages/decisions/$s.md" wiki/index.md)" = 1 || exit 1; done` exits 0
    - `grep -c "wiki/pages/decisions/" .planning/PROJECT.md` prints 8
    - `tail -n 2 wiki/log.md | head -n 1 | grep -q "| DECISION |"` and `tail -n 1 wiki/log.md | grep -q "| LINT |"` both exit 0
    - `D=$(git diff e954363 -- wiki/log.md) && test "$(printf '%s\n' "$D" | grep -c '^-[^-]')" = 0` exits 0 (no removed log lines)
    - `grep -q "_(none yet)_" wiki/index.md` exits 0
    - `ls wiki/pages/*/ | grep -E "\.md$" | sort | uniq -d` prints nothing (no duplicate slugs)
  </acceptance_criteria>
  <done>All 7 Key Decisions have ADR pages; index.md catalogs all 16 pages in the required order; PROJECT.md links rows 1-8 to the wiki; log.md has appended DECISION and LINT lines; wiki-lint passes against e954363. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wiki/ to public repo | Every page and log line is published (D-12) |
| raw sources to LLM-written pages | Provenance of facts depends on raw/ staying immutable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-26 | Tampering | wiki/raw/ provenance | medium | mitigate | wiki-lint --base fails on any change to wiki/raw since e954363 (RAW_MODIFIED) |
| T-01-27 | Repudiation | wiki/log.md audit trail | medium | mitigate | wiki-lint --base requires the e954363 log content as an exact prefix (LOG_REWRITTEN); acceptance counts zero removed lines |
| T-01-28 | Information Disclosure | wiki pages in the public repo | medium | mitigate | wiki-lint SENSITIVE check rejects CG- key patterns and email addresses in pages |
</threat_model>

<verification>
- `node scripts/wiki-lint.mjs --base e954363` prints pages=16 orphans=0 broken_links=0 duplicates=0 errors=0
- Decisions ordering check prints the expected 8 slugs in order
- CLAUDE.md contains the wiki-lint phase-transition rule
</verification>

<success_criteria>
- ROADMAP success criterion 4: wiki/index.md, wiki/log.md, wiki/SCHEMA.md exist and the job posting and CoinGecko notes are ingested (verified by lint on the existing sources) with Key Decisions backfilled
- MEM-01..04 verified by a repeatable script and CLAUDE.md rule; D-14 completed without rebuilding existing pages
</success_criteria>

## Edge Coverage (spec-less probe fallback)

- MEM-02 / adjacency — resolved (explicit): unique slugs, no duplicate of foundation-skeleton-conventions.
- MEM-02 / empty — resolved (explicit): all category headings kept, empty ones show `_(none yet)_`.
- MEM-02 / ordering — resolved (explicit): existing Decisions bullet first, new bullets in PROJECT.md order.
- MEM-04 / idempotency — resolved (explicit): deterministic lint output; each decision path exactly once in index.md.
- MEM-04 / concurrency — resolved (explicit): single wiki writer per wave; e954363 log content stays a byte-identical prefix.

## Flagged Assumptions (unresolved probe items — not silently dropped)

- MEM-01 (unclassified, unresolved): assumed that "follows Karpathy's LLM Wiki pattern" is satisfied structurally (immutable raw/, LLM-owned pages/ with SCHEMA frontmatter, SCHEMA.md, index, append-only log) as checked by wiki-lint. Page quality (concise, factual, cited) is not machine-checked; it relies on the human read at phase transitions.
- MEM-03 (unclassified, unresolved): assumed that "parseable prefixes" means the SCHEMA.md line regex, with FINDING accepted as a fifth prefix because SCHEMA.md allows it although MEM-03 lists four. A literal pipe inside a summary is not escaped and is assumed never to occur.

## Prohibitions (spec-less recall, flagged-unverified)

- MEM-01/MEM-03 / transparency: raw sources and past log lines are never edited (test-tier; enforced in practice by wiki-lint --base, but the must_haves item is descriptor-less).
- MEM-02 / transparency: unverified CoinGecko facts keep their (unverified) marker (judgment-tier).
- MEM-02 / privacy: no keys, secrets or email addresses in public wiki pages (test-tier; wiki-lint SENSITIVE check).

## Artifacts this phase produces

Files: scripts/wiki-lint.mjs; wiki/pages/decisions/paper-trading-scope.md, limit-order-fill-model.md, tech-stack.md, coingecko-proxy.md, qa-first-class.md, llm-wiki-memory.md, free-hosting-public-repo.md; modified wiki/index.md, wiki/log.md, .planning/PROJECT.md, .claude/CLAUDE.md.
CLI flags: `--pages-only`, `--base REV` (scripts/wiki-lint.mjs).
Lint error codes: LAYER_MISSING, PAGE_BAD_DIR, FRONTMATTER, DUPLICATE_SLUG, BROKEN_LINK, SENSITIVE, INDEX_HEADING, ORPHAN, INDEX_DUPLICATE, INDEX_CATEGORY, INDEX_DEAD, INDEX_EMPTY, LOG_FORMAT, RAW_MODIFIED, LOG_REWRITTEN.
Summary line: `wiki-lint: pages=N orphans=N broken_links=N duplicates=N unverified=N errors=N`.

<output>
Create `.planning/phases/01-foundation-project-memory/01-06-SUMMARY.md` when done.
</output>
