---
phase: 01-foundation-project-memory
plan: 05
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - web/src/lib/api.ts
  - web/src/lib/api.test.ts
  - web/src/lib/healthPoller.ts
  - web/src/lib/healthPoller.test.ts
  - web/src/components/HealthBadge.tsx
  - web/src/components/HealthBadge.test.tsx
  - web/src/App.tsx
  - web/src/App.test.tsx
  - web/src/pages/ComingSoon.tsx
  - web/src/styles/theme.css
  - web/src/main.tsx
autonomous: true
requirements: [FND-01, FND-03]
coupling_justified:
  - "01-03: web/ and api/ file sets are disjoint; the smoke test picks free ports, so parallel smoke runs cannot collide"
  - "01-04: web/ and api/ file sets are disjoint; the web mirrors the frozen /health contract and does not depend on 01-04 internals"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The web app shows a dark Binance-style shell: a top nav with Markets, Trade, Wallet and Orders links, a main area, and a footer with the health badge and a Powered by CoinGecko link (D-01)"
    - "Each of /markets, /trade, /wallet and /orders renders its own title and a coming-soon message; / redirects to /markets; an unknown path renders a Page not found message inside the same shell"
    - "The badge fetches GET /health on load and then every 60 seconds only while document.visibilityState is visible; while the tab is hidden no request is made; on becoming visible it fetches immediately if 60 seconds or more have passed since the last fetch, otherwise at the remaining time (D-02)"
    - "The badge displays the API status and the CoinGecko status as one of ok, degraded, down, not configured, each with a distinct colour token (D-02)"
    - "When GET /health fails, the badge shows API unreachable and, when the error body or X-Request-Id header provides one, Request ID followed by that ID, so a bug report can quote it (D-09 thread)"
    - "Clicking the badge's Re-check button triggers exactly one immediate /health fetch; clicking again while that fetch is in flight starts no second request (walking-skeleton interactive element)"
    - "Stopping the poller (component unmount, including React StrictMode's double mount in dev) aborts any in-flight request, clears the pending timer and removes the visibility listener; no state update happens afterwards"
  artifacts:
    - path: "web/src/lib/healthPoller.ts"
      provides: "Visibility-aware polling state machine with injected timers and visibility adapter"
      exports: ["createHealthPoller", "HealthPoller", "HealthState", "VisibilityAdapter", "HEALTH_POLL_INTERVAL_MS"]
    - path: "web/src/components/HealthBadge.tsx"
      provides: "Pure HealthBadgeView plus HealthBadge container wiring the poller to document visibility"
      exports: ["HealthBadge", "HealthBadgeView"]
    - path: "web/src/App.tsx"
      provides: "BrowserRouter app, AppLayout shell and appRoutes route objects"
      exports: ["App", "AppLayout", "appRoutes"]
    - path: "web/src/pages/ComingSoon.tsx"
      provides: "Placeholder page used by the four routes and the not-found route"
      exports: ["ComingSoon"]
    - path: "web/src/styles/theme.css"
      provides: "Dark theme custom properties and shell layout"
      contains: "--color-bg"
  key_links:
    - from: "web/src/components/HealthBadge.tsx"
      to: "web/src/lib/healthPoller.ts"
      via: "useEffect creates the poller with a document visibility adapter and stops it on cleanup"
      pattern: "createHealthPoller\\("
    - from: "web/src/lib/healthPoller.ts"
      to: "web/src/lib/api.ts"
      via: "injected fetchHealth performs GET /health"
      pattern: "fetchHealth"
    - from: "web/src/App.tsx"
      to: "web/src/components/HealthBadge.tsx"
      via: "AppLayout footer renders HealthBadge"
      pattern: "<HealthBadge"
  prohibitions:
    - statement: "MUST NOT call the CoinGecko API directly or hold any market-data API key in the browser bundle; the web app talks only to the project API"
      status: resolved
      verification: test
---

<objective>
Build the D-01 dark app shell that later phases fill in (nav with four placeholder routes, footer with health badge and CoinGecko attribution) and the D-02 health badge that polls only while the tab is visible, shows both API and CoinGecko status, surfaces the request ID on failure, and offers a Re-check button.

Purpose: FND-01 success criterion 1 as a reviewer sees it ("the web page shows API health status"), and the walking skeleton's interactive UI element wired to the API.

Output: web/src/lib/healthPoller.ts (+ test), HealthBadge view/container (+ test), App shell with react-router routes (+ test), ComingSoon page, theme.css.
</objective>

## Phase Goal

ROADMAP Goal (verbatim): "A running skeleton (web + api) with CI and traceable logs, plus the wiki memory and test strategy every later phase builds on". User story framing: NOT AVAILABLE (the Goal line is not in As a / I want to / so that form; run `/gsd mvp-phase 1` to add it; the planner did not invent one).

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-project-memory/01-CONTEXT.md
@.planning/phases/01-foundation-project-memory/SKELETON.md
@.planning/phases/01-foundation-project-memory/01-01-SUMMARY.md

<interfaces>
Frozen by plan 01-01 (keep these signatures; internals may change):
- web/src/lib/api.ts: const API_BASE_URL: string; type UpstreamStatus = 'ok' | 'degraded' | 'down' | 'not_configured'; interface HealthResponse { status: 'ok'; version: string; commit: string; upstream: { coingecko: { status: UpstreamStatus; checkedAt: string | null; latencyMs: number | null } } }; interface ApiResult of T { data: T; requestId: string | null }; class ApiError extends Error { status: number | null; code: string; requestId: string | null }; function fetchHealth(options?: { signal?: AbortSignal; fetchImpl?: typeof fetch }): Promise of ApiResult of HealthResponse
- API error body (from D-09, implemented in plan 01-03): { error: { code: string; message: string; requestId: string } } with the same ID in the X-Request-Id header
- react-router 8.4.0 exports used here (verified present): BrowserRouter, MemoryRouter, Routes, Route, NavLink, Navigate, Outlet, useRoutes, matchRoutes, type RouteObject
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Visibility-aware health poller, request-ID error parsing and the badge with a Re-check button</name>
  <files>web/src/lib/api.ts, web/src/lib/api.test.ts, web/src/lib/healthPoller.ts, web/src/lib/healthPoller.test.ts, web/src/components/HealthBadge.tsx, web/src/components/HealthBadge.test.tsx</files>
  <read_first>
    - web/src/lib/api.ts
    - web/src/lib/api.test.ts (existing tests must keep passing)
    - web/src/components/HealthBadge.tsx (tracer version to replace)
    - web/vite.config.ts (test environment is node; no DOM library is installed)
    - .planning/phases/01-foundation-project-memory/01-CONTEXT.md (D-02, D-09)
  </read_first>
  <behavior>
    - Poller start with visibility visible: fetchHealth called once immediately; after advancing fake time 60000 ms a second call; onUpdate receives kind ok with data and requestId after each success
    - Tab hidden after the first fetch: advancing 180000 ms makes no further call
    - Tab becomes visible 90000 ms after the last fetch started: one immediate call; becomes visible 20000 ms after the last fetch started: no immediate call, one call 40000 ms later
    - refresh() while idle: one immediate call and the 60000 ms schedule restarts from that call; refresh() twice while the first fetch is pending: still exactly one call
    - fetchHealth rejecting with an ApiError whose requestId is req-123: onUpdate receives kind error carrying that error, and polling continues at the next interval
    - stop(): the pending fetch's AbortSignal is aborted, no timer fires afterwards, the visibility subscription is removed, and onUpdate is never called after stop even if the aborted promise settles
    - fetchHealth (api.ts) on a 500 whose body is { error: { code: 'INTERNAL_ERROR', message: 'Internal Server Error', requestId: 'r-1' } } rejects with ApiError status 500, code INTERNAL_ERROR, requestId r-1; on a 502 with a non-JSON body and header x-request-id r-2 rejects with code HTTP_502 and requestId r-2; on an aborted signal rethrows the AbortError instead of NETWORK_ERROR
    - HealthBadgeView (renderToStaticMarkup): loading shows Checking API; ok with coingecko not_configured shows API ok and CoinGecko: not configured with data-status not_configured; each of ok, degraded, down renders its own data-status value; error with requestId r-9 shows API unreachable and Request ID: r-9; error without requestId shows no Request ID text; the Re-check button has type button and is disabled when checking is true
  </behavior>
  <action>
Sizing note: six files, three of which are tests. Write the tests first (RED): the poller cases in `web/src/lib/healthPoller.test.ts`, the view cases in `web/src/components/HealthBadge.test.tsx`, and the three fetchHealth error-parsing cases appended to the existing `web/src/lib/api.test.ts` (keep its 01-01 cases). Tests run in Vitest's node environment: use vi.useFakeTimers for the poller and `renderToStaticMarkup` from react-dom/server for the view. Do not add jsdom or any testing library (not vetted by the 01-01 package gate).
Update `web/src/lib/api.ts` (per D-09) keeping its exported signatures: on a non-2xx response try to parse JSON; when it has error.code as a string, throw ApiError with that code, error.message, and requestId from error.requestId or else the x-request-id header; otherwise throw ApiError with code HTTP_ followed by the status and the header request ID; when fetch rejects because the signal was aborted, rethrow the original error; any other rejection becomes ApiError(null, NETWORK_ERROR, null).
Create `web/src/lib/healthPoller.ts` (per D-02): export HEALTH_POLL_INTERVAL_MS = 60000; type HealthState = { kind: 'loading' } or { kind: 'ok'; data: HealthResponse; requestId: string | null } or { kind: 'error'; error: ApiError }; interface VisibilityAdapter { isVisible(): boolean; subscribe(listener: () => void): () => void }; interface HealthPoller { start(): void; stop(): void; refresh(): Promise of void; isChecking(): boolean }; `createHealthPoller({ fetchHealth, onUpdate, visibility, intervalMs = HEALTH_POLL_INTERVAL_MS, now = Date.now, timers = { setTimeout, clearTimeout } })` implementing exactly the behaviour list: one AbortController per fetch, at most one in-flight fetch, a single pending timer scheduled only while visible, lastFetchStartedAt drives the on-visible decision, and a stopped flag that suppresses every later onUpdate. Errors that are aborts are swallowed; other errors emit kind error and polling continues.
Rewrite `web/src/components/HealthBadge.tsx`: export `HealthBadgeView({ state, checking, onRecheck })` as a pure component rendering a container with data-testid health-badge; loading text Checking API…; ok text API ok plus a span with data-status equal to the upstream status and text CoinGecko: followed by the label (not_configured shown as not configured), with a title attribute containing the API version, commit and checkedAt (or never); error text API unreachable plus, when error.requestId exists, a span with text Request ID: and the ID; and a button with type button, text Re-check, aria-label Re-check API health, disabled when checking. Render all values as React text children only (never inject raw HTML). Export `HealthBadge()` as the container: in useEffect create the poller with fetchHealth from api.ts, a visibility adapter backed by document.visibilityState and the visibilitychange event, and onUpdate storing state; call start, and return a cleanup that calls stop (this makes StrictMode's double mount safe). The Re-check button calls poller.refresh and reflects isChecking.
Run `npx prettier --write` and `npx eslint` on the changed files.
  </action>
  <verify>
    <automated>npm --prefix web run test</automated>
    <fails_when>non-zero exit, or "No test files found", or any test file reported as failed</fails_when>
    <automated>npm --prefix web run typecheck</automated>
    <fails_when>non-zero exit, or any "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -q "visibilitychange" web/src/components/HealthBadge.tsx && grep -q "createHealthPoller" web/src/components/HealthBadge.tsx` exits 0
    - `grep -Eq "HEALTH_POLL_INTERVAL_MS = 60_?000" web/src/lib/healthPoller.ts` exits 0
    - `grep -rn "dangerouslySetInnerHTML" web/src` prints nothing
    - `grep -rniE "x-cg-demo-api-key|coingecko_api_key|api\.coingecko\.com" web/src web/index.html web/vite.config.ts` prints nothing (browser never calls CoinGecko or carries its key)
    - `node -e "const p=require('./web/package.json'); const d={...p.dependencies,...p.devDependencies}; if(d.jsdom||d['happy-dom']||d['@testing-library/react']) process.exit(1)"` exits 0
    - `grep -cE "(it|test)\(" web/src/lib/healthPoller.test.ts` prints 6 or more
  </acceptance_criteria>
  <done>The badge polls /health on load and every 60 s only while visible, shows API and CoinGecko status, shows the request ID on failure, and has a Re-check button that never overlaps requests; poller, api client and view are covered by passing node-environment tests. Committed.</done>
</task>

<task type="auto">
  <name>Task 2: Dark Binance-style shell with Markets / Trade / Wallet / Orders placeholder routes and CoinGecko attribution</name>
  <files>web/src/App.tsx, web/src/App.test.tsx, web/src/pages/ComingSoon.tsx, web/src/styles/theme.css, web/src/main.tsx</files>
  <read_first>
    - web/src/App.tsx (tracer version)
    - web/src/main.tsx
    - web/src/components/HealthBadge.tsx (from Task 1)
    - .planning/phases/01-foundation-project-memory/01-CONTEXT.md (D-01)
    - .planning/phases/01-foundation-project-memory/SKELETON.md (Frontend and Styling rows)
  </read_first>
  <action>
Per D-01. Create `web/src/styles/theme.css` with custom properties on :root: --color-bg #0b0e11, --color-surface #181a20, --color-border #2b3139, --color-text #eaecef, --color-muted #848e9c, --color-accent #f0b90b, --color-up #0ecb81, --color-down #f6465d; body uses the background and text colours, a system sans-serif font stack and no margin; classes app-shell (full-height column grid: header, main, footer), app-nav (horizontal flex bar on the surface colour with a bottom border), nav-link and nav-link.active (active link in the accent colour), app-main (padded content), app-footer (flex row, space-between, muted text, top border), health-badge, and status tones via attribute selectors on data-status: ok uses --color-up, degraded uses --color-accent, down uses --color-down, not_configured uses --color-muted.
Create `web/src/pages/ComingSoon.tsx` exporting `ComingSoon({ title, description })` rendering a section with an h1 of title and a paragraph with description.
Rewrite `web/src/App.tsx`: export `AppLayout` rendering div.app-shell with a header nav.app-nav containing a brand link to /markets with text CoinGecko Paper Exchange and NavLinks to /markets (Markets), /trade (Trade), /wallet (Wallet), /orders (Orders) using className nav-link plus active when active; main.app-main with Outlet; footer.app-footer containing HealthBadge and an anchor to https://www.coingecko.com with text Powered by CoinGecko, target _blank and rel noopener noreferrer. Export `appRoutes` as a RouteObject array with one parent route element AppLayout and children: index route with element Navigate to /markets replace; markets with ComingSoon title Markets and description "Coming soon: live markets table (Phase 3)"; trade with title Trade and "Coming soon: price chart and order panel (Phases 3-5)"; wallet with title Wallet and "Coming soon: balances and portfolio value (Phase 4)"; orders with title Orders and "Coming soon: open orders and history (Phase 5)"; and a catch-all path * with title Page not found and description "This page does not exist." Export an internal AppRoutes component calling useRoutes(appRoutes) and export `App` wrapping it in BrowserRouter. Update `web/src/main.tsx` to import ./styles/theme.css before rendering App inside StrictMode.
Create `web/src/App.test.tsx` (node environment, renderToStaticMarkup): for each of /markets, /trade, /wallet, /orders render AppRoutes inside MemoryRouter with that initial entry and assert the markup contains the page title, the text Coming soon, all four nav hrefs, and the Powered by CoinGecko link with rel noopener noreferrer; render /nope and assert Page not found inside the shell; for the redirect use matchRoutes(appRoutes, '/') and assert the matched leaf route is the index route whose element is a Navigate element with props.to /markets (a static render cannot run the redirect effect).
Run `npx prettier --write`, `npx eslint`, `npm --prefix web run test`, `npm --prefix web run typecheck`, `npm --prefix web run build`, and finally `npm run smoke`.
  </action>
  <verify>
    <automated>npm --prefix web run test -- src/App.test.tsx</automated>
    <fails_when>non-zero exit, or "No test files found", or the summary line lacks "passed"</fails_when>
    <automated>npm --prefix web run build</automated>
    <fails_when>non-zero exit, or output contains "error" from vite or rolldown</fails_when>
    <automated>npm run smoke</automated>
    <fails_when>non-zero exit, or output lacks "SMOKE OK"</fails_when>
    <human-check>
      <test>From the repo root run `npm run dev`, then open http://localhost:5173 in Chrome with DevTools Network filtered to "health"</test>
      <expected>1) Dark background with a yellow-accented top nav: Markets, Trade, Wallet, Orders; / lands on /markets and the active link is highlighted. 2) Each nav link shows its own title and a Coming soon line. 3) Footer: badge reads "API ok" and "CoinGecko: not configured" (or ok/degraded/down in its colour if api/.env has a key) plus a "Powered by CoinGecko" link opening coingecko.com in a new tab. 4) Exactly one /health request on load; switch to another tab for over 60 s and no /health requests appear; return and one request fires immediately. 5) Clicking Re-check fires one /health request and the button is disabled until it completes. 6) Stop the API process: within the next poll or Re-check the badge shows "API unreachable".</expected>
      <why_human>Visual styling, tab-visibility behaviour in a real browser and the network timeline cannot be asserted by node-environment tests</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `grep -q "noopener noreferrer" web/src/App.tsx && grep -q "Powered by CoinGecko" web/src/App.tsx` exits 0
    - `grep -q "/markets" web/src/App.tsx && grep -q "/trade" web/src/App.tsx && grep -q "/wallet" web/src/App.tsx && grep -q "/orders" web/src/App.tsx` exits 0
    - `grep -q "theme.css" web/src/main.tsx && grep -q "StrictMode" web/src/main.tsx` exits 0
    - `grep -q -- "--color-accent" web/src/styles/theme.css && grep -q "data-status" web/src/styles/theme.css` exits 0
    - `npm --prefix web run typecheck` and `npm --prefix web run build` exit 0
  </acceptance_criteria>
  <done>The dark shell with four placeholder routes, redirect, not-found page and footer (badge + attribution) renders under test and builds; smoke passes; the visual and visibility checks are queued for the end-of-phase human review. Committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response to browser DOM | /health fields and error messages are rendered in the page |
| browser to external site | Attribution link opens coingecko.com |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-22 | Tampering (XSS) | HealthBadgeView rendering of API fields | low | mitigate | Values rendered only as React text children; acceptance greps web/src for any raw-HTML injection prop |
| T-01-23 | Information Disclosure | web bundle | high | mitigate | No market-data key or CoinGecko endpoint in web/; acceptance grep over web/src, index.html and vite.config.ts; the browser calls only API_BASE_URL (prohibition in must_haves) |
| T-01-24 | Tampering (reverse tabnabbing) | Powered by CoinGecko link | low | mitigate | target _blank with rel noopener noreferrer, asserted in App.test.tsx |
| T-01-25 | Denial of Service | badge polling | low | mitigate | 60 s interval only while visible, single in-flight request, Re-check disabled while checking; the API's 5-minute upstream cache absorbs any remaining load |
</threat_model>

<verification>
- `npm --prefix web run test`, `npm --prefix web run typecheck`, `npm --prefix web run build` pass
- `npm run smoke` passes
- End-of-phase human check (harvested by the verifier into UAT): shell visuals, routes, badge states, visibility-aware polling, Re-check
</verification>

<success_criteria>
- ROADMAP success criterion 1 (web half): the web page shows API health status, including CoinGecko's ok / degraded / down / not configured
- D-01 shell and D-02 polling behaviour implemented and covered by tests; later phases fill the four routes without restyling
</success_criteria>

## Prohibitions (spec-less recall, flagged-unverified)

- FND-01 / safety: the browser never calls CoinGecko or carries its key (must_haves.prohibitions; descriptor-less).

## Artifacts this phase produces

Files: web/src/lib/healthPoller.ts, web/src/lib/healthPoller.test.ts, web/src/components/HealthBadge.test.tsx, web/src/App.test.tsx, web/src/pages/ComingSoon.tsx, web/src/styles/theme.css; modified web/src/lib/api.ts, web/src/lib/api.test.ts, web/src/components/HealthBadge.tsx, web/src/App.tsx, web/src/main.tsx.
Symbols: HEALTH_POLL_INTERVAL_MS, HealthState, VisibilityAdapter, HealthPoller, createHealthPoller (web/src/lib/healthPoller.ts); HealthBadgeView, HealthBadge (web/src/components/HealthBadge.tsx); AppLayout, appRoutes, App (web/src/App.tsx); ComingSoon (web/src/pages/ComingSoon.tsx).
CSS: custom properties --color-bg, --color-surface, --color-border, --color-text, --color-muted, --color-accent, --color-up, --color-down; classes app-shell, app-nav, nav-link, app-main, app-footer, health-badge; data-status tone selectors.
Routes: /, /markets, /trade, /wallet, /orders, catch-all not-found.

<output>
Create `.planning/phases/01-foundation-project-memory/01-05-SUMMARY.md` when done.
</output>
